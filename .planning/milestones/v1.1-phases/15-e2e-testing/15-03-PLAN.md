---
phase: 15-e2e-testing
plan: 03
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - e2e/marketplace.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [E2E-05, E2E-06]

must_haves:
  truths:
    - "Marketplace browse test navigates to /marketplace/browse and verifies the page heading"
    - "Marketplace test verifies filter bar is rendered with sort/type controls"
    - "Marketplace test navigates to a listing detail page and verifies content"
    - "CI workflow has an 'e2e' job that installs Playwright browsers and runs npx playwright test"
    - "CI e2e job uses dummy Supabase env vars -- no real secrets injected"
    - "CI e2e job uploads both playwright-report/ and playwright-results/ as artifacts on failure"
  artifacts:
    - path: "e2e/marketplace.spec.ts"
      provides: "E2E tests for marketplace: browse page, filters, listing detail navigation"
      min_lines: 40
    - path: ".github/workflows/ci.yml"
      provides: "Updated CI workflow with e2e job running Playwright tests fully offline"
      contains: "playwright"
  key_links:
    - from: "e2e/marketplace.spec.ts"
      to: "/marketplace/browse"
      via: "page.goto to marketplace browse page"
      pattern: "page\\.goto.*marketplace/browse"
    - from: ".github/workflows/ci.yml"
      to: "npx playwright test"
      via: "e2e job step running Playwright"
      pattern: "npx playwright test"
---

<objective>
Write E2E test for marketplace browse (search, filter, listing detail) and integrate all E2E tests into the CI pipeline as a required check -- fully offline with dummy Supabase env vars (no real secrets).

Purpose: Complete the E2E test coverage for all four critical user journeys and ensure E2E failures block PR merges, all running without real Supabase dependency.
Output: Marketplace spec passing locally, CI workflow updated with e2e job using dummy env vars.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-e2e-testing/15-CONTEXT.md
@.planning/phases/15-e2e-testing/15-RESEARCH.md
@.planning/phases/15-e2e-testing/15-01-SUMMARY.md

<interfaces>
<!-- Marketplace browse page (src/app/(marketplace)/marketplace/browse/page.tsx) -->
<!-- Server Component with server-side Supabase query -->

Page structure:
- h1 heading: "Browse Marketplace" (default, no search/type params)
- h1 heading: Search: "query" when ?q= param present
- h1 heading: typeConfig.label when ?type= param present
- MarketplaceFilterBar component with totalCount prop
- ListingsGrid component rendering listing cards
- Pagination component when totalCount > 18

URL params: ?type=, ?sort=, ?q=, ?page=
Sort options: newest (default), price_asc, price_desc, rating, popular

<!-- Marketplace listing detail: src/app/(marketplace)/marketplace/[slug]/ -->
<!-- Individual listing pages at /marketplace/[slug] -->

<!-- MarketplaceFilterBar (src/components/marketplace/filter-bar.tsx) -->
<!-- Client component with search input, sort dropdown, type filter buttons -->

<!-- FULLY OFFLINE STRATEGY -->
<!-- Browse page is RSC with server-side Supabase calls. page.route() cannot intercept these. -->
<!-- Dev server runs with dummy NEXT_PUBLIC_SUPABASE_URL from playwright.config.ts. -->
<!-- Server-side queries will fail gracefully. Tests verify page structure and client-side interactions: -->
<!--   - Heading rendering (works even if no listings returned) -->
<!--   - Filter bar controls (client component, renders regardless of data) -->
<!--   - URL parameter handling (server-side URL parsing works independently of DB) -->

<!-- CI workflow (.github/workflows/ci.yml) -->
<!-- Existing jobs: lint, typecheck, test (all parallel on ubuntu-latest, Node 24) -->
<!-- New e2e job: uses dummy Supabase env vars hardcoded in workflow (NOT secrets) -->
<!-- No real Supabase secrets injected -- tests are fully offline -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write marketplace browse E2E test</name>
  <files>e2e/marketplace.spec.ts</files>
  <action>
    Create `e2e/marketplace.spec.ts` with test.describe('Marketplace'):

    Architecture note: Marketplace browse is a Server Component. Supabase query happens server-side and cannot be intercepted by Playwright. The dev server uses dummy Supabase env vars, so server queries fail gracefully. Tests verify page structure and URL parameter handling -- the page shell, heading, and filter bar render even with zero listings.

    Tests:

    1. Test: 'browse page loads with heading and filter bar'
       - Navigate to /marketplace/browse
       - Verify heading: await expect(page.getByRole('heading', { name: /browse marketplace/i })).toBeVisible()
       - Verify filter bar is present (look for sort control or filter elements)
       - The page should render its shell even with zero listings

    2. Test: 'search via URL parameter shows search heading'
       - Navigate to /marketplace/browse?q=test
       - Verify heading changes to show the search term
       - This tests the server-side URL parameter parsing (works independently of DB data)

    3. Test: 'type filter via URL parameter shows filtered heading'
       - Navigate to /marketplace/browse?type=api_endpoint
       - Verify heading shows the type label from LISTING_TYPE_MAP
       - This tests server-side type filtering parameter handling

    4. Test: 'sort parameter changes page without error'
       - Navigate to /marketplace/browse?sort=price_asc
       - Verify page loads successfully (heading visible)
       - Navigate to /marketplace/browse?sort=popular
       - Verify page loads successfully
       - This validates all sort options are handled without crashing

    5. Test: 'clicking a listing navigates to detail page'
       - Navigate to /marketplace/browse
       - Check if any listing links exist: const listingLink = page.locator('a[href*="/marketplace/"]').filter({ hasNot: page.locator('a[href="/marketplace/browse"]') }).first()
       - If count > 0, click the first listing link and verify URL changes to /marketplace/slug pattern
       - If no listings (offline / dummy env vars), skip this test gracefully
       - Use test.skip condition based on listing count

    Handle empty state gracefully: tests should pass by verifying page structure (heading, filter bar) even when no listings exist in the database.
  </action>
  <verify>
    <automated>cd "F:/BotProject/AI Market Cap" && npx playwright test e2e/marketplace.spec.ts --project=chromium-desktop 2>&1 | tail -20</automated>
  </verify>
  <done>Marketplace spec has 5 tests covering browse page load, search heading, type filter heading, sort parameter handling, and listing detail navigation. Tests pass fully offline with dummy Supabase env vars, handling empty data with skip conditions where needed.</done>
</task>

<task type="auto">
  <name>Task 2: Add E2E job to CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Read the existing .github/workflows/ci.yml file first. Then update it to add an e2e job alongside existing lint/typecheck/test jobs.

    Add the following job block after the existing test job:

    e2e:
      name: E2E
      runs-on: ubuntu-latest
      timeout-minutes: 15
      env:
        NEXT_PUBLIC_SUPABASE_URL: 'https://test.supabase.co'
        NEXT_PUBLIC_SUPABASE_ANON_KEY: 'test-anon-key'
      steps:
        - uses: actions/checkout@v4
        - uses: actions/setup-node@v4
          with:
            node-version: 24
            cache: 'npm'
        - run: npm ci
        - name: Install Playwright Browsers
          run: npx playwright install --with-deps chromium firefox
        - name: Run E2E tests
          run: npx playwright test
        - uses: actions/upload-artifact@v4
          if: failure()
          with:
            name: playwright-report
            path: playwright-report/
            retention-days: 7
        - uses: actions/upload-artifact@v4
          if: failure()
          with:
            name: playwright-results
            path: playwright-results/
            retention-days: 7

    Key details:
    - Separate job that runs in parallel with lint/typecheck/test (no depends_on/needs)
    - timeout-minutes: 15 for dev server startup + browser tests
    - DUMMY Supabase env vars hardcoded directly in the workflow (NOT from secrets)
      'https://test.supabase.co' and 'test-anon-key' allow Next.js to start without errors.
      All Supabase calls are either intercepted by page.route() or fail gracefully.
      No real Supabase secrets needed -- tests are fully offline.
    - --with-deps flag installs OS-level browser dependencies on ubuntu-latest
    - Upload BOTH playwright-report/ AND playwright-results/ as separate artifacts on failure only
      (playwright-report/ has the HTML report, playwright-results/ has screenshots and trace files)
    - Uses actions/upload-artifact@v4 (v3 is deprecated)
    - E2E is a required status check -- note to user: must configure in GitHub branch protection settings manually

    Preserve all existing content. Only ADD the new e2e job block.
  </action>
  <verify>
    <automated>cd "F:/BotProject/AI Market Cap" && node -e "const fs=require('fs'); const c=fs.readFileSync('.github/workflows/ci.yml','utf8'); if(!c.includes('playwright')) throw 'missing playwright'; if(!c.includes('e2e:')) throw 'missing e2e job'; if(!c.includes('upload-artifact@v4')) throw 'missing artifact upload'; if(!c.includes('playwright-results')) throw 'missing playwright-results artifact'; if(c.includes('secrets.NEXT_PUBLIC_SUPABASE')) throw 'must NOT use real secrets'; if(!c.includes('test.supabase.co')) throw 'missing dummy supabase URL'; console.log('CI yml contains e2e job with dummy env vars, playwright, and both artifact uploads')"</automated>
  </verify>
  <done>CI workflow has 4 parallel jobs (lint, typecheck, test, e2e). E2E job installs Playwright browsers with --with-deps, uses dummy Supabase env vars (no real secrets), runs npx playwright test, and uploads both playwright-report/ and playwright-results/ on failure. YAML is valid and all existing jobs are preserved.</done>
</task>

</tasks>

<verification>
- npx playwright test e2e/marketplace.spec.ts --project=chromium-desktop passes all tests
- .github/workflows/ci.yml contains e2e job with dummy Supabase env vars (no secrets.NEXT_PUBLIC_SUPABASE_*)
- CI e2e job uploads both playwright-report/ and playwright-results/ as artifacts on failure
- Existing CI jobs (lint, typecheck, test) are unchanged
- YAML syntax is valid
</verification>

<success_criteria>
- Marketplace E2E tests cover browse page structure, search/type/sort URL params, and listing navigation
- CI workflow updated with e2e job running fully offline (dummy Supabase env vars, no secrets)
- CI uploads both report and results artifacts on failure (screenshots + trace files)
- All 4 spec files (auth, model-detail, leaderboard, marketplace) can run together: npx playwright test
</success_criteria>

<output>
After completion, create `.planning/phases/15-e2e-testing/15-03-SUMMARY.md`
</output>
