---
phase: 13-component-decomposition-react-memo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/compare/compare-client.tsx
  - src/app/compare/_components/model-selector.tsx
  - src/app/compare/_components/comparison-row.tsx
  - src/app/compare/_components/overview-table.tsx
  - src/app/compare/_components/benchmarks-table.tsx
  - src/app/compare/_components/pricing-table.tsx
  - src/app/compare/_components/visual-comparison.tsx
autonomous: true
requirements: [DECOMP-02, PERF-02]

must_haves:
  truths:
    - "Compare page renders model selectors, overview table, benchmarks table, pricing table, and visual charts"
    - "Adding/removing a model updates the comparison table correctly"
    - "ComparisonRow wrapped in React.memo skips re-render when its primitive props are unchanged"
    - "No component file exceeds 300 lines"
    - "All existing tests pass after decomposition"
  artifacts:
    - path: "src/app/compare/compare-client.tsx"
      provides: "Parent shell with state management, model fetching, and layout"
      max_lines: 300
    - path: "src/app/compare/_components/model-selector.tsx"
      provides: "Dropdown model picker with search and selection"
      min_lines: 50
    - path: "src/app/compare/_components/comparison-row.tsx"
      provides: "React.memo wrapped table row with value highlighting"
      contains: "memo(function ComparisonRow"
    - path: "src/app/compare/_components/overview-table.tsx"
      provides: "Overview comparison section (quality scores, provider, parameters)"
      min_lines: 40
  key_links:
    - from: "src/app/compare/compare-client.tsx"
      to: "_components/model-selector.tsx"
      via: "import and render per model slot"
      pattern: "import.*ModelSelector.*from.*_components"
    - from: "src/app/compare/_components/overview-table.tsx"
      to: "_components/comparison-row.tsx"
      via: "import and render ComparisonRow for each metric"
      pattern: "import.*ComparisonRow.*from.*comparison-row"
---

<objective>
Decompose the compare client (718 lines) into focused sub-components under 300 lines, and apply React.memo to ComparisonRow for re-render optimization.

Purpose: The compare page has natural extraction points -- ModelSelector (already ~97 lines inline) and ComparisonRow (already ~40 lines inline) are defined as functions inside the file. The 4 comparison table sections (overview, benchmarks, pricing, visual) are each Card sections that can be extracted. ComparisonRow is the top React.memo candidate since it renders N times per model and parent re-renders on model add/remove, but individual row props often remain stable.

Output: compare-client.tsx reduced to ~200 lines (state + layout), 6 sub-component files in `_components/`, ComparisonRow wrapped with React.memo.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-component-decomposition-react-memo/13-RESEARCH.md

Key context:
- compare-client.tsx is a CLIENT component ("use client")
- ModelSelector is defined as a standalone function inside compare-client.tsx (lines 56-152)
- ComparisonRow is defined as a standalone function inside compare-client.tsx (lines 155-197)
- Both are already well-isolated -- just need to move to separate files
- The supabase client is created at module scope (line 31-34) -- keep it in compare-client.tsx or move to a shared import
- Helper functions getBenchmarkScore, getCheapestPrice, getSpeed (lines 275-294) can stay in compare-client.tsx or move to a helpers file
- The BenchmarkScoreWithBenchmarks type alias (line 29) and ModelOption/CompareClientProps interfaces must go where needed
- Types: ModelWithDetails, BenchmarkScore, ModelPricing, Benchmark imported from @/types/database
- React.memo on ComparisonRow: values prop is an array -- parent should use useMemo to stabilize array references for memo to be effective
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ModelSelector and ComparisonRow into sub-components</name>
  <files>
    src/app/compare/_components/model-selector.tsx,
    src/app/compare/_components/comparison-row.tsx
  </files>
  <action>
    Create `_components/` directory under `src/app/compare/`.

    1. **model-selector.tsx** (~100 lines): Move the ModelSelector function (lines 56-152) to its own file. Add "use client" directive. Define its props interface:
    ```
    interface ModelSelectorProps {
      allModels: ModelOption[];
      selectedSlugs: string[];
      onSelect: (slug: string) => void;
      placeholder?: string;
    }
    ```
    Move the ModelOption interface here (or define it here and export it for the parent to import).
    Import: useState, useRef, useEffect from react; CATEGORY_MAP from constants; ProviderLogo; lucide icons (Check, ChevronsUpDown, X).
    Export as named export: `export function ModelSelector(...)`.

    2. **comparison-row.tsx** (~50 lines): Move the ComparisonRow function (lines 155-197) to its own file. Add "use client" directive. Wrap with React.memo:
    ```typescript
    import { memo } from "react";
    import { Trophy } from "lucide-react";

    interface ComparisonRowProps {
      label: string;
      values: (string | number | null)[];
      highlight?: "max" | "min" | null;
    }

    export const ComparisonRow = memo(function ComparisonRow({
      label,
      values,
      highlight,
    }: ComparisonRowProps) {
      // ... existing row rendering logic
    });
    ```
    The memo wrapping is effective here because: the parent re-renders when models change, but individual ComparisonRow instances with unchanged label/values/highlight can skip re-rendering. The parent will need to use useMemo for the values arrays passed to ComparisonRow.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ModelSelector and ComparisonRow exist as separate files in _components/. ComparisonRow is wrapped with React.memo. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extract comparison table sections and rewire parent</name>
  <files>
    src/app/compare/compare-client.tsx,
    src/app/compare/_components/overview-table.tsx,
    src/app/compare/_components/benchmarks-table.tsx,
    src/app/compare/_components/pricing-table.tsx,
    src/app/compare/_components/visual-comparison.tsx
  </files>
  <action>
    Extract the 4 Card sections from the comparison table area (lines ~380-718) into sub-components. All are "use client" components.

    1. **overview-table.tsx** (~100 lines): The Overview Card (lines 382-480). Props: models array, ComparisonRow component already imported. Renders ComparisonRow for: Quality Score, Provider, Category, Parameters, Context Window, Release Date, Open Weights. Uses helper functions for formatting -- import formatParams, formatContextWindow, formatNumber from @/lib/format. Import ComparisonRow from ./comparison-row.

    2. **benchmarks-table.tsx** (~50 lines): The Benchmarks Card (lines 482-522). Props: models array, allBenchmarks array. Renders ComparisonRow for each benchmark. Uses getBenchmarkScore helper -- pass as prop or import. Import ComparisonRow from ./comparison-row.

    3. **pricing-table.tsx** (~60 lines): The Pricing & Speed Card (lines 525-588). Props: models array. Renders ComparisonRow for Input Price, Output Price, Speed (tok/s), TTFT. Uses getCheapestPrice, getSpeed helpers and formatTokenPrice. Import ComparisonRow from ./comparison-row.

    4. **visual-comparison.tsx** (~80 lines): The Visual Comparison section (lines ~590-718). Props: models array. Renders BenchmarkRadarOverlay, PriceComparison, SpeedCostScatter charts. Import chart components from their existing locations.

    Rewrite compare-client.tsx:
    - Keep "use client", keep all state (models, selectedSlugs, loading), keep fetchModel, addModel, removeModel
    - Keep supabase client creation at module scope
    - Keep the header/breadcrumb area and model selector bar
    - Move helper functions (getBenchmarkScore, getCheapestPrice, getSpeed) to compare-client.tsx or inline them in the sub-components that use them. Simplest approach: keep them in compare-client.tsx and pass pre-computed values as props to the table sub-components. OR move them to a small `_components/compare-helpers.ts` file.
    - Keep allBenchmarks computation in the parent (depends on models state)
    - Import and render: ModelSelector, OverviewTable, BenchmarksTable, PricingTable, VisualComparison
    - Move the BenchmarkScoreWithBenchmarks type to compare-helpers.ts or to the benchmarks-table sub-component
    - Export CompareClient and ModelOption interface from this file (ModelOption may also be exported from model-selector.tsx)

    For React.memo effectiveness on ComparisonRow: In the table sub-components, use useMemo for the values arrays passed to each ComparisonRow. Example:
    ```typescript
    const qualityValues = useMemo(
      () => models.map(m => m.quality_score ? Number(m.quality_score).toFixed(1) : null),
      [models]
    );
    ```
    This ensures ComparisonRow gets stable references and can skip re-renders.

    Target: compare-client.tsx under 200 lines.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>compare-client.tsx is under 300 lines. 4 table section sub-components exist. React.memo on ComparisonRow is wired with useMemo'd values arrays. TypeScript compiles clean. All tests pass.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` passes with zero errors
2. `npx vitest run` passes all existing tests
3. `wc -l src/app/compare/compare-client.tsx` shows under 300 lines
4. `grep -r "memo(function ComparisonRow" src/app/compare/_components/` confirms React.memo wrapping
5. All sub-component files exist in `_components/` directory
</verification>

<success_criteria>
- Compare client decomposed from 718 lines to under 300 lines with 6 sub-components
- ComparisonRow wrapped with React.memo
- Values arrays passed to ComparisonRow use useMemo in parent table components
- All "use client" directives present on extracted client components
- TypeScript compiles clean, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-component-decomposition-react-memo/13-02-SUMMARY.md`
</output>
