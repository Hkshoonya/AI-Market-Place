---
phase: 13-component-decomposition-react-memo
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/search-dialog.tsx
  - src/components/search-dialog-results.tsx
  - src/components/search-dialog-default.tsx
  - src/components/charts/rank-timeline.tsx
  - src/components/charts/rank-timeline-controls.tsx
  - src/components/charts/rank-timeline-tags.tsx
  - src/components/models/models-filter-bar.tsx
  - src/components/models/filter-sheet-content.tsx
autonomous: true
requirements: [DECOMP-04]

must_haves:
  truths:
    - "Search dialog renders search results and default state (recent searches, categories) correctly"
    - "Rank timeline renders controls, tracked model tags, and chart area correctly"
    - "Models filter bar renders desktop filters and mobile filter sheet correctly"
    - "No component file exceeds 300 lines"
    - "Existing search-dialog.test.tsx passes without import changes"
    - "All existing tests pass after decomposition"
  artifacts:
    - path: "src/components/search-dialog.tsx"
      provides: "Dialog shell with state management, keyboard shortcuts, search debouncing"
      max_lines: 300
    - path: "src/components/search-dialog-results.tsx"
      provides: "Model results + marketplace results list"
      min_lines: 40
    - path: "src/components/charts/rank-timeline.tsx"
      provides: "Parent with state, data fetching, and Recharts rendering"
      max_lines: 300
    - path: "src/components/models/models-filter-bar.tsx"
      provides: "Filter bar with desktop controls and sheet trigger"
      max_lines: 300
  key_links:
    - from: "src/components/search-dialog.tsx"
      to: "search-dialog-results.tsx"
      via: "import and render when query has results"
      pattern: "import.*SearchDialogResults.*from.*search-dialog-results"
    - from: "src/components/search-dialog.tsx"
      to: "search-dialog-default.tsx"
      via: "import and render when no query (default state)"
      pattern: "import.*SearchDialogDefault.*from.*search-dialog-default"
    - from: "src/components/charts/rank-timeline.tsx"
      to: "rank-timeline-controls.tsx"
      via: "import and render header controls"
      pattern: "import.*RankTimelineControls.*from.*rank-timeline-controls"
---

<objective>
Decompose search-dialog (485 lines), rank-timeline (501 lines), and models-filter-bar (470 lines) below 300 lines each.

Purpose: These three src/components/ files are part of DECOMP-04 (top 5 mega-components). Each has clear extraction points: search-dialog has results vs default-state rendering, rank-timeline has controls/tags vs chart rendering, and models-filter-bar has a ~200-line Sheet content panel that can be extracted.

Output: 3 parent files reduced under 300 lines each, with 5 sub-component files created alongside them.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-component-decomposition-react-memo/13-RESEARCH.md

Key context:
- search-dialog.tsx has an existing test: src/components/search-dialog.test.tsx (224 lines). Test imports `{ SearchDialog } from './search-dialog'`. This import MUST continue to work after decomposition. Do NOT change the export location.
- search-dialog.tsx defines 3 module-level helper functions (getRecentSearches, addRecentSearch, clearRecentSearches) that are not exported. These can stay in search-dialog.tsx or move to a helper if needed by sub-components.
- rank-timeline.tsx is a "use client" component with its own data fetching via fetch() to /api/charts/rank-timeline. The chart rendering (Recharts LineChart) and data fetching stay in the parent. Controls (metric toggle, days selector, model input) and tags (tracked model badges with remove buttons) are extracted.
- models-filter-bar.tsx uses useSearchParams, useRouter, useTransition for URL-driven filtering. The Sheet component content (mobile filter panel, lines ~180-470) is the extraction target. The desktop filter bar (~180 lines) stays in the parent.
- Sub-components for search-dialog go in src/components/ (same directory, co-located). Sub-components for rank-timeline go in src/components/charts/. Sub-components for models-filter-bar go in src/components/models/.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose search-dialog and rank-timeline</name>
  <files>
    src/components/search-dialog.tsx,
    src/components/search-dialog-results.tsx,
    src/components/search-dialog-default.tsx,
    src/components/charts/rank-timeline.tsx,
    src/components/charts/rank-timeline-controls.tsx,
    src/components/charts/rank-timeline-tags.tsx
  </files>
  <action>
    **Search Dialog decomposition:**

    1. **search-dialog-results.tsx** (~120 lines): Extract the search results rendering section (model results list + marketplace results list). "use client" component. Props:
    ```typescript
    interface SearchDialogResultsProps {
      results: SearchResult[];
      marketplaceResults: MarketplaceResult[];
      activeIndex: number;
      onNavigateModel: (slug: string) => void;
      onNavigateMarketplace: (slug: string) => void;
    }
    ```
    Move the SearchResult and MarketplaceResult interfaces here (export them so parent can import the types). Contains the results mapping with active highlighting, provider logos, category badges, marketplace listing cards. Import ProviderLogo, Badge, CATEGORY_MAP as needed.

    2. **search-dialog-default.tsx** (~80 lines): Extract the default state rendering (shown when no query). "use client" component. Props:
    ```typescript
    interface SearchDialogDefaultProps {
      recentSearches: string[];
      onSearchRecent: (query: string) => void;
      onClearRecent: () => void;
      onClose: () => void;
    }
    ```
    Contains recent searches list with click handlers, category quick-link buttons, "clear" button. Import CATEGORIES from constants.

    3. Rewrite **search-dialog.tsx** (~200 lines): Keep the dialog shell, all state management (query, results, marketplaceResults, loading, activeIndex, recentSearches), keyboard shortcut handler, focus management, debounced search function, navigation functions. Import and render SearchDialogResults when query has results, SearchDialogDefault when no query. Keep the helper functions (getRecentSearches, addRecentSearch, clearRecentSearches) in this file. CRITICAL: Keep `export function SearchDialog()` as the named export from this file -- the test imports from './search-dialog'.

    **Rank Timeline decomposition:**

    1. **rank-timeline-controls.tsx** (~80 lines): Extract the header area with metric toggle (rank/score), days selector (7/14/30/90), and model input field with add button. "use client" component. Props:
    ```typescript
    interface RankTimelineControlsProps {
      metric: "rank" | "score";
      setMetric: (m: "rank" | "score") => void;
      days: number;
      setDays: (d: number) => void;
      inputValue: string;
      setInputValue: (v: string) => void;
      onAddSlug: () => void;
      onKeyDown: (e: React.KeyboardEvent<HTMLInputElement>) => void;
    }
    ```

    2. **rank-timeline-tags.tsx** (~40 lines): Extract the tracked model tags section (colored badges with remove buttons). "use client" component. Props:
    ```typescript
    interface RankTimelineTagsProps {
      slugs: string[];
      models: ModelInfo[];
      onRemoveSlug: (slug: string) => void;
    }
    ```
    Import LINE_COLORS constant (move to this file or keep in parent and pass as needed). Simpler: move LINE_COLORS to rank-timeline-tags.tsx and also export it so parent can import it for the chart line colors.

    3. Rewrite **rank-timeline.tsx** (~200 lines): Keep all state, data fetching (fetchData, useEffect), Y-axis domain computation, Recharts LineChart rendering, CustomTooltip component. Import and render RankTimelineControls and RankTimelineTags. Keep the DEFAULT_SLUGS constant. Import LINE_COLORS from rank-timeline-tags.tsx. Export ModelInfo interface if needed by sub-components.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>search-dialog.tsx under 250 lines with 2 sub-component files. rank-timeline.tsx under 250 lines with 2 sub-component files. Existing search-dialog.test.tsx passes unchanged. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Decompose models-filter-bar</name>
  <files>
    src/components/models/models-filter-bar.tsx,
    src/components/models/filter-sheet-content.tsx
  </files>
  <action>
    Extract the Sheet content (mobile filter panel) from models-filter-bar.tsx into a separate sub-component.

    **filter-sheet-content.tsx** (~200 lines): Extract the SheetContent children -- all the filter sections rendered inside the mobile filter sheet (category select, provider select, parameter range select, license input, open weights toggle, has API toggle, clear filters button). "use client" component. Props:
    ```typescript
    interface FilterSheetContentProps {
      currentCategory: string;
      currentProvider: string;
      currentParams: string;
      currentLicense: string;
      currentOpenOnly: boolean;
      currentHasApi: boolean;
      updateParams: (updates: Record<string, string>) => void;
    }
    ```
    Import the constants used by filters: SORT_OPTIONS, PROVIDER_OPTIONS, PARAM_RANGES -- these can be defined in this file or imported from the parent. Cleanest: move these constants to filter-sheet-content.tsx since they're primarily used by the sheet filter UI. Export them if the parent desktop bar also uses SORT_OPTIONS (it does for the sort dropdown).

    Actually, looking at the structure: SORT_OPTIONS is used in the desktop bar too (the sort Select). So keep SORT_OPTIONS in models-filter-bar.tsx. Move PROVIDER_OPTIONS and PARAM_RANGES to filter-sheet-content.tsx (only used in sheet).

    Rewrite **models-filter-bar.tsx** (~270 lines): Keep the desktop filter bar rendering (search input, category buttons, sort select, view toggle, filter sheet trigger button with active count badge). Keep all state derivation from searchParams, updateParams helper, debounced search. Import FilterSheetContent and render it inside the Sheet/SheetContent wrapper. Keep SORT_OPTIONS in this file.

    Target: models-filter-bar.tsx under 300 lines.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>models-filter-bar.tsx under 300 lines with filter-sheet-content.tsx extracted. All filter functionality preserved. TypeScript compiles clean. All tests pass (including existing filter-bar.test.tsx for marketplace filter bar -- not the same component but verify no collateral damage).</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` passes with zero errors
2. `npx vitest run` passes all existing tests -- specifically search-dialog.test.tsx must pass unchanged
3. `wc -l src/components/search-dialog.tsx src/components/charts/rank-timeline.tsx src/components/models/models-filter-bar.tsx` all show under 300 lines
4. Sub-component files exist: search-dialog-results.tsx, search-dialog-default.tsx, rank-timeline-controls.tsx, rank-timeline-tags.tsx, filter-sheet-content.tsx
</verification>

<success_criteria>
- search-dialog.tsx decomposed from 485 to under 250 lines, SearchDialog export path unchanged
- rank-timeline.tsx decomposed from 501 to under 250 lines
- models-filter-bar.tsx decomposed from 470 to under 300 lines
- search-dialog.test.tsx passes without any modifications
- TypeScript compiles clean, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-component-decomposition-react-memo/13-03-SUMMARY.md`
</output>
