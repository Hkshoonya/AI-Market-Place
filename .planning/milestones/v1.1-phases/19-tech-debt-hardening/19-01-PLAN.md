---
phase: 19-tech-debt-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/schemas/index.ts
autonomous: true
requirements: [E2E-03, E2E-06, CICD-01, SIMP-01, SIMP-02]
must_haves:
  truths:
    - "msw is a direct devDependency in package.json, not just transitive"
    - "npm run lint exits non-zero when ESLint warnings exist"
    - "src/lib/schemas/index.ts barrel file no longer exists"
    - "listing-reviews.tsx has no unused useEffect import (pre-resolved)"
    - "npx tsc --noEmit and vitest run both pass clean"
  artifacts:
    - path: "package.json"
      provides: "msw devDependency + lint --max-warnings 0 script"
      contains: "msw"
    - path: "package-lock.json"
      provides: "Updated lockfile with msw as direct devDependency"
  key_links:
    - from: "e2e/mocks/server.ts"
      to: "package.json devDependencies.msw"
      via: "npm ci installs msw directly"
      pattern: "msw"
    - from: ".github/workflows/ci.yml npm run lint"
      to: "package.json scripts.lint"
      via: "lint script includes --max-warnings 0"
      pattern: "max-warnings 0"
---

<objective>
Close four integration fragility gaps and tech debt items identified in the v1.1 milestone re-audit: promote msw to a direct devDependency, enforce zero ESLint warnings in the lint script, delete the orphaned schemas barrel file, and confirm the useEffect import issue was already resolved.

Purpose: Harden CI reliability by making implicit dependencies explicit and enforcing the zero-warning baseline established in Phase 16.
Output: Updated package.json (msw devDep + lint script), deleted barrel file, clean tsc + vitest.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-tech-debt-hardening/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote msw to direct devDependency and enforce zero-warning lint</name>
  <files>package.json, package-lock.json</files>
  <action>
Two changes to package.json, one via npm CLI and one manual edit:

1. **MSW devDependency (closes INT-MSW-DEP, hardens E2E-03/E2E-06):**
   Run `npm install -D msw@2.12.10` from project root. This promotes the existing transitive msw (installed via shadcn) to a direct devDependency. Pin to 2.12.10 to match the exact version currently in the lockfile and avoid unintended upgrades.

   After running, verify `package.json` devDependencies contains `"msw"` entry.

2. **Lint script --max-warnings 0 (closes INT-LINT-GATE, hardens CICD-01):**
   In `package.json`, change the `"lint"` script from:
   ```json
   "lint": "eslint"
   ```
   to:
   ```json
   "lint": "eslint --max-warnings 0"
   ```
   This ensures `npm run lint` (which CI runs via `ci.yml`) exits non-zero if any ESLint warnings exist, enforcing the zero-warning baseline from Phase 16. No change to `ci.yml` is needed -- CI already runs `npm run lint`.

   Research confirmed: `npm run lint -- --max-warnings 0` currently exits with code 0 (zero warnings exist), so this change will not break existing CI.
  </action>
  <verify>
    <automated>cd "F:/BotProject/AI Market Cap" && node -e "const p=require('./package.json'); const hasMsw=!!p.devDependencies?.msw; const hasFlag=p.scripts?.lint?.includes('--max-warnings 0'); console.log('msw devDep:', hasMsw, '| lint flag:', hasFlag); if(!hasMsw||!hasFlag) process.exit(1)" && npm run lint</automated>
  </verify>
  <done>package.json devDependencies contains msw@^2.12.10 and lint script is "eslint --max-warnings 0". npm run lint exits 0 (zero warnings).</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned schemas barrel and confirm useEffect pre-resolution</name>
  <files>src/lib/schemas/index.ts</files>
  <action>
Two cleanup items (closes SIMP-01, SIMP-02):

1. **Delete orphaned barrel file:**
   Delete `src/lib/schemas/index.ts`. Research confirmed zero callers import from `@/lib/schemas` or `@/lib/schemas/index` -- all 40+ callers import from sub-paths like `@/lib/schemas/parse`, `@/lib/schemas/marketplace`, etc.

   Run `rm src/lib/schemas/index.ts` then `npx tsc --noEmit` to confirm no broken imports. TypeScript will report any files that fail to resolve the barrel.

2. **Confirm useEffect pre-resolution (no action needed):**
   The audit flagged `listing-reviews.tsx` as having an unused `useEffect` import. Research verified this was already resolved during Phase 14 SWR conversion -- `listing-reviews.tsx` currently imports only `useState` from React.

   Run a verification grep to confirm: `grep -c "useEffect" src/components/marketplace/listing-reviews.tsx` should return 0.

   Document in the SUMMARY that this item was pre-resolved during Phase 14.

After both items: run `npx tsc --noEmit` and `npm test` to confirm nothing is broken.
  </action>
  <verify>
    <automated>cd "F:/BotProject/AI Market Cap" && test ! -f src/lib/schemas/index.ts && echo "barrel deleted" && grep -c "useEffect" src/components/marketplace/listing-reviews.tsx | grep -q "0" && echo "no useEffect import" && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>src/lib/schemas/index.ts no longer exists. listing-reviews.tsx has zero useEffect references. npx tsc --noEmit passes clean. npm test (222+ tests) passes clean.</done>
</task>

</tasks>

<verification>
All four audit items are resolved:

1. INT-MSW-DEP: `node -e "const p=require('./package.json');process.exit(p.devDependencies.msw?0:1)"` exits 0
2. INT-LINT-GATE: `npm run lint` uses --max-warnings 0 and exits 0
3. SIMP-02 barrel: `test ! -f src/lib/schemas/index.ts` passes
4. SIMP-02 useEffect: `grep -c useEffect src/components/marketplace/listing-reviews.tsx` returns 0

Phase gate: `npm run lint && npm test && npx tsc --noEmit` all exit 0.
</verification>

<success_criteria>
- msw appears in package.json devDependencies
- package.json lint script is "eslint --max-warnings 0"
- src/lib/schemas/index.ts does not exist
- listing-reviews.tsx has no useEffect import
- npx tsc --noEmit exits 0
- npm test exits 0 (all 222+ tests pass)
- npm run lint exits 0 (zero warnings)
</success_criteria>

<output>
After completion, create `.planning/phases/19-tech-debt-hardening/19-01-SUMMARY.md`
</output>
