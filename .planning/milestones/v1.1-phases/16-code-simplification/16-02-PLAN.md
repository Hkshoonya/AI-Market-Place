---
phase: 16-code-simplification
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/app/(auth)/profile/profile-content.tsx
  - src/components/search-dialog.tsx
  - src/components/three/ambient-scene.tsx
  - src/components/three/scene-content.tsx
  - src/hooks/use-auction-timer.ts
  - src/app/(marketplace)/marketplace/auctions/auctions-browse-content.tsx
  - src/components/charts/trading-chart.tsx
  - src/app/(auth)/orders/[id]/order-detail-content.tsx
  - src/app/page.tsx
  - src/components/models/leaderboard-explorer.tsx
  - eslint.config.mjs
autonomous: true
requirements: [SIMP-01, SIMP-02]

must_haves:
  truths:
    - "All React compiler warnings (set-state-in-effect, purity, immutability, exhaustive-deps) are fixed via refactoring, not suppression"
    - "The incompatible-library warning is handled with a targeted eslint-disable with justification comment"
    - "eslint.config.mjs warning overrides for react-hooks/set-state-in-effect, purity, and immutability are removed"
    - "npx eslint . --max-warnings 0 passes clean (zero warnings, zero errors)"
    - "All 222 vitest tests still pass"
    - "TypeScript compiles clean"
    - "Three.js scenes still render (no visual breakage from immutability/purity fixes)"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "Strict React compiler enforcement (no warn overrides)"
      contains: "no set-state-in-effect warn override"
    - path: "src/app/(auth)/profile/profile-content.tsx"
      provides: "set-state-in-effect fixed via useState initializer"
      contains: "useState"
    - path: "src/components/three/ambient-scene.tsx"
      provides: "Math.random calls moved to useMemo"
      contains: "useMemo"
    - path: "src/components/three/scene-content.tsx"
      provides: "Math.random calls moved to useMemo, ref mutation fixed"
      contains: "useMemo"
  key_links:
    - from: "eslint.config.mjs"
      to: "CI pipeline"
      via: "removed warn overrides = errors enforced"
      pattern: "no set-state-in-effect.*warn"
    - from: "all modified files"
      to: "npx eslint . --max-warnings 0"
      via: "zero warnings enforcement"
      pattern: "0 warnings"
---

<objective>
Fix all React compiler lint warnings via proper refactoring and remove the warning downgrade overrides from eslint.config.mjs to make violations CI-blocking errors permanently.

Purpose: Pay off the warning debt created in Phase 10 (CI baseline) by fixing the root causes of all ~33 React compiler warnings. This is the higher-risk batch requiring behavioral changes -- each file is verified individually after changes.
Output: Zero ESLint warnings project-wide. React compiler rules enforced at error level in CI.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-code-simplification/16-CONTEXT.md
@.planning/phases/16-code-simplification/16-RESEARCH.md
@.planning/phases/16-code-simplification/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix set-state-in-effect and exhaustive-deps warnings across 6 files</name>
  <files>
    src/app/(auth)/profile/profile-content.tsx,
    src/components/search-dialog.tsx,
    src/components/three/ambient-scene.tsx,
    src/hooks/use-auction-timer.ts,
    src/app/(marketplace)/marketplace/auctions/auctions-browse-content.tsx,
    src/components/charts/trading-chart.tsx,
    src/app/(auth)/orders/[id]/order-detail-content.tsx
  </files>
  <action>
Fix each file individually, running `npx tsc --noEmit` after each to catch regressions early.

**1. profile-content.tsx:89 — set-state-in-effect**
Read the file. The pattern is useState("") + useEffect that calls setDisplayName/setUsername/setBio when profile loads. Fix: initialize useState with the profile value directly:
```
const [displayName, setDisplayName] = useState(profile?.display_name ?? "");
```
Remove the synchronous setState useEffect entirely. If the profile loads asynchronously and updates later, use a `key` prop on the parent component keyed to `profile?.id` to force remount with fresh initial values, OR use `useRef` to track if initial values have been set.

**2. search-dialog.tsx:52 — set-state-in-effect (debounce)**
The `setDebouncedQuery("")` in the `query.length < 2` branch is synchronous setState in an effect. Fix: make the reset go through the timeout path too — use `setTimeout(() => setDebouncedQuery(""), 0)` for the reset case, or restructure so ALL paths use the same timeout/clearTimeout pattern. Do NOT break the debounce UX behavior.

**3. search-dialog.tsx:85 — set-state-in-effect (open effect)**
The `setRecentSearches(getRecentSearches())` and `setQuery/setDebouncedQuery/setActiveIndex` calls when dialog opens are synchronous setState. Fix options (choose the cleanest):
- Initialize `recentSearches` with `useState(() => getRecentSearches())` (lazy initializer) and track `open` transitions with `useRef` to refresh only when open goes from false to true
- Or use `useReducer` to batch all resets into a single dispatch
- Or use a `key` prop on the dialog content keyed to a counter that increments on open

**4. ambient-scene.tsx:129 — set-state-in-effect**
Read the file. This is a Three.js/R3F component. The setState call is likely inside a useEffect that sets up the scene. Fix: move state initialization outside the effect (to useMemo or useState initializer) if possible. If it's an R3F-specific pattern where setState MUST happen in effect (e.g., after scene mount), use `queueMicrotask` or `startTransition` to make it async.

**5. use-auction-timer.ts:61 — set-state-in-effect**
Read the file to understand if this is a setInterval callback or direct effect body call. If setInterval: the setState is already async (in a callback), which is acceptable — the fix may be to ensure the effect cleanup properly. If direct effect body: move to useState initializer or useReducer. Apply the appropriate fix.

**6. auctions-browse-content.tsx:289 — exhaustive-deps**
The `auctions` logical expression in the deps array is unstable. Fix: stabilize with useMemo:
```typescript
const stableAuctions = useMemo(() => auctions ?? [], [auctions]);
```
Then use `stableAuctions` in the effect dependency array.

**7. trading-chart.tsx:67 — exhaustive-deps**
`chartData` conditional in deps. Fix: stabilize `chartData` reference with useMemo:
```typescript
const stableChartData = useMemo(() => chartData ?? [], [chartData]);
```
Use `stableChartData` in effect deps.

**8. order-detail-content.tsx:85 — exhaustive-deps**
`messages` logical expression in deps. Same pattern: stabilize with useMemo.

After ALL files are fixed, run `npx tsc --noEmit && npx vitest run --reporter=dot` to verify no regressions.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=dot</automated>
  </verify>
  <done>All set-state-in-effect warnings (5 instances across 4 files) and all exhaustive-deps warnings (3 instances across 3 files) resolved via refactoring. TypeScript compiles clean. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix purity, immutability, and incompatible-library warnings, then remove eslint overrides</name>
  <files>
    src/app/page.tsx,
    src/components/three/ambient-scene.tsx,
    src/components/three/scene-content.tsx,
    src/components/models/leaderboard-explorer.tsx,
    eslint.config.mjs
  </files>
  <action>
Fix remaining React compiler warnings, then remove the warning overrides to enforce errors permanently.

**1. page.tsx:419 — purity (Date.now)**
Move `Date.now()` call into a `useMemo`:
```typescript
const timestamp = useMemo(() => Date.now(), []);
```
Or hoist to a module-level constant if the component is only rendered once.

**2. ambient-scene.tsx:21-33 — purity (Math.random x8)**
Move ALL `Math.random()` calls (lines 21,22,23,27,28,29,32,33) into a single useMemo:
```typescript
const randomValues = useMemo(() => ({
  // all 8 random values computed once
  val1: Math.random() * ...,
  val2: Math.random() * ...,
  // etc.
}), []);
```
Destructure and use throughout the component.

**3. ambient-scene.tsx:53 — immutability (ref mutation)**
The ref mutation in the R3F animation callback. Read the exact code at line 53. If it's inside a `useFrame` callback (R3F animation loop that runs outside React render), the proper fix is to copy the ref value to a local variable before mutating:
```typescript
const mesh = meshRef.current;
if (mesh) { mesh.rotation.y += delta; }
```
If ESLint still flags this pattern because it detects `.current` access, apply a targeted `// eslint-disable-next-line react-hooks/immutability -- R3F useFrame runs outside React render cycle` with justification comment.

**4. scene-content.tsx:26-47 — purity (Math.random x9)**
Same pattern as ambient-scene. Move ALL `Math.random()` calls (lines 26,27,28,33,38,39,40,44,47) into a single useMemo computed once on mount.

**5. scene-content.tsx:121,191 — immutability (ref mutation)**
Same pattern as ambient-scene. Copy ref value to local variable before mutating. Add targeted eslint-disable if needed for R3F useFrame patterns.

**6. leaderboard-explorer.tsx:246 — incompatible-library**
Read line 246 to identify the exact third-party hook. This warning means the React compiler cannot optimize this code because it uses an incompatible library. Per research: add a targeted eslint-disable with justification:
```typescript
// eslint-disable-next-line react-hooks/incompatible-library -- [library name] predates React compiler compatibility; cannot change library internals
```

**7. FINAL STEP: Remove eslint.config.mjs warning overrides**
ONLY after ALL warnings above are verified fixed. Read `eslint.config.mjs`, find and DELETE the entire rules block that contains:
```javascript
{
  rules: {
    "react-hooks/set-state-in-effect": "warn",
    "react-hooks/purity": "warn",
    "react-hooks/immutability": "warn",
  },
},
```
This promotes all three rules back to their default error level. Any future violations will block CI.

**8. VERIFICATION: Run `npx eslint . --max-warnings 0`**
This must pass with ZERO warnings and ZERO errors. If any warning remains, fix it before considering this task done.

Also run full suite: `npx tsc --noEmit && npx vitest run --reporter=dot`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=dot && npx eslint . --max-warnings 0</automated>
  </verify>
  <done>All React compiler warnings fixed. eslint.config.mjs overrides removed. `npx eslint . --max-warnings 0` passes (zero warnings, zero errors). All 222 tests pass. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
After both tasks, the entire codebase must pass:
1. `npx tsc --noEmit` — zero errors
2. `npx vitest run` — all 222 tests pass
3. `npx eslint . --max-warnings 0` — zero warnings, zero errors (the strictest possible gate)
4. `npx playwright test` — all 37 E2E tests pass (final phase gate)
</verification>

<success_criteria>
- All ~33 React compiler warnings resolved via proper refactoring (not suppression, except incompatible-library which requires targeted disable)
- eslint.config.mjs no longer has warning downgrades for set-state-in-effect, purity, or immutability
- `npx eslint . --max-warnings 0` passes clean
- `npx tsc --noEmit` passes clean
- All 222 vitest tests pass
- All 37 Playwright E2E tests pass
- The warning debt from Phase 10 is fully paid off
</success_criteria>

<output>
After completion, create `.planning/phases/16-code-simplification/16-02-SUMMARY.md`
</output>
