---
phase: 16-code-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/error.tsx
  - src/app/(catalog)/skills/page.tsx
  - src/app/(rankings)/leaderboards/[category]/page.tsx
  - src/app/(rankings)/leaderboards/page.tsx
  - src/app/api/admin/bootstrap/route.ts
  - src/app/api/webhooks/chain-deposits/route.ts
  - src/app/not-found.tsx
  - src/components/compare/share-comparison.tsx
  - src/components/layout/header.tsx
  - src/components/layout/market-ticker.test.tsx
  - src/components/marketplace/english-bid-panel.tsx
  - src/components/marketplace/wallet-deposit-panel.tsx
  - src/components/models/ranking-weight-controls.test.tsx
  - src/components/models/ranking-weight-controls.tsx
  - src/components/models/similar-models.tsx
  - src/components/notifications/notification-bell.tsx
  - src/components/three/scene-content.tsx
  - src/lib/compute-scores/compute-all-lenses.test.ts
  - src/lib/compute-scores/fetch-inputs.test.ts
  - src/lib/compute-scores/persist-results.test.ts
  - src/lib/data-sources/adapters/artificial-analysis.ts
  - src/lib/data-sources/adapters/livebench.ts
  - src/lib/data-sources/adapters/open-llm-leaderboard.ts
  - src/lib/marketplace/auctions/dutch.ts
  - src/lib/payments/chains/evm.ts
  - src/lib/scoring/market-cap-calculator.ts
  - supabase/functions/sync-huggingface/index.ts
  - src/components/search-dialog.tsx
  - src/app/(marketplace)/marketplace/auctions/[id]/auction-detail-content.tsx
autonomous: true
requirements: [SIMP-02]

must_haves:
  truths:
    - "All ~40 unused-import/variable ESLint warnings are resolved"
    - "Accessibility aria-props warning on search-dialog combobox is fixed"
    - "Next.js no-img-element warning on auction-detail-content is fixed"
    - "No test regressions -- all 222 vitest tests still pass"
    - "TypeScript compiles clean with npx tsc --noEmit"
  artifacts:
    - path: "src/app/(auth)/error.tsx"
      provides: "Unused Home import removed"
      contains: "// REMOVED"
    - path: "src/components/search-dialog.tsx"
      provides: "aria-expanded and aria-controls added to combobox"
      contains: "aria-expanded"
    - path: "src/app/(marketplace)/marketplace/auctions/[id]/auction-detail-content.tsx"
      provides: "img replaced with Next.js Image"
      contains: "import Image"
  key_links:
    - from: "all modified files"
      to: "npx tsc --noEmit"
      via: "TypeScript compilation"
      pattern: "no errors"
    - from: "all modified files"
      to: "vitest run"
      via: "test regression check"
      pattern: "222 tests pass"
---

<objective>
Mechanical cleanup of all unused imports, unused variables, dead code, accessibility warning, and Next.js img warning across v1.1-touched files.

Purpose: Eliminate ~40 low-risk lint warnings that are purely mechanical removals or simple replacements. This is the safe batch that requires no behavioral changes.
Output: All Category A (unused vars/imports), Category C (aria), and Category D (img) warnings from the research inventory resolved.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-code-simplification/16-CONTEXT.md
@.planning/phases/16-code-simplification/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused imports, variables, and dead code across all flagged files</name>
  <files>
    src/app/(auth)/error.tsx,
    src/app/(catalog)/skills/page.tsx,
    src/app/(rankings)/leaderboards/[category]/page.tsx,
    src/app/(rankings)/leaderboards/page.tsx,
    src/app/api/admin/bootstrap/route.ts,
    src/app/api/webhooks/chain-deposits/route.ts,
    src/app/not-found.tsx,
    src/components/compare/share-comparison.tsx,
    src/components/layout/header.tsx,
    src/components/layout/market-ticker.test.tsx,
    src/components/marketplace/english-bid-panel.tsx,
    src/components/marketplace/wallet-deposit-panel.tsx,
    src/components/models/ranking-weight-controls.test.tsx,
    src/components/models/ranking-weight-controls.tsx,
    src/components/models/similar-models.tsx,
    src/components/notifications/notification-bell.tsx,
    src/components/three/scene-content.tsx,
    src/lib/compute-scores/compute-all-lenses.test.ts,
    src/lib/compute-scores/fetch-inputs.test.ts,
    src/lib/compute-scores/persist-results.test.ts,
    src/lib/data-sources/adapters/artificial-analysis.ts,
    src/lib/data-sources/adapters/livebench.ts,
    src/lib/data-sources/adapters/open-llm-leaderboard.ts,
    src/lib/marketplace/auctions/dutch.ts,
    src/lib/payments/chains/evm.ts,
    src/lib/scoring/market-cap-calculator.ts,
    supabase/functions/sync-huggingface/index.ts
  </files>
  <action>
Work through every unused-import/variable warning from the research inventory. Per user decision, use `// REMOVED` tag convention:

**Unused imports** (comment out with `// REMOVED` tag):
- `src/app/(auth)/error.tsx:6` — `Home` import
- `src/app/(catalog)/skills/page.tsx:19` — `formatNumber` import
- `src/app/(catalog)/skills/page.tsx:141` — `DeploymentRow` local type (comment out entire type def)
- `src/app/(rankings)/leaderboards/[category]/page.tsx:5` — `CardHeader`, `CardTitle` imports
- `src/app/(rankings)/leaderboards/[category]/page.tsx:11` — `formatTokenPrice`, `formatNumber` imports
- `src/app/(rankings)/leaderboards/page.tsx:11` — `formatNumber` import
- `src/app/api/webhooks/chain-deposits/route.ts:4` — `Token` import
- `src/app/not-found.tsx:2` — `ArrowLeft` import
- `src/components/compare/share-comparison.tsx:4` — `Link2` import
- `src/components/layout/header.tsx:12` — `Search` import
- `src/components/marketplace/english-bid-panel.tsx:13` — `Badge` import
- `src/components/models/similar-models.tsx:3` — `Badge` import
- `src/components/models/similar-models.tsx:6` — `formatNumber` import
- `src/components/notifications/notification-bell.tsx:8` — `Check` import
- `src/lib/data-sources/adapters/artificial-analysis.ts:9` — `sanitizeFilterValue`, `sanitizeSlug` imports
- `src/lib/data-sources/adapters/livebench.ts:26` — `sanitizeFilterValue`, `sanitizeSlug` imports
- `src/lib/marketplace/auctions/dutch.ts:9` — `refundEscrow` import

**Unused assigned variables** (comment out with `// REMOVED` tag):
- `src/components/models/similar-models.tsx:30` — `catConfig` (comment out assignment, keep any destructured use check)
- `src/components/three/scene-content.tsx:15` — `viewport` (comment out the destructure)
- `src/lib/data-sources/adapters/livebench.ts:122` — `offset` (comment out assignment)
- `src/lib/data-sources/adapters/open-llm-leaderboard.ts:353` — `shortName` (comment out assignment)
- `src/lib/payments/chains/evm.ts:68` — `TRANSFER_EVENT_SIGNATURE` (comment out const)
- `src/lib/scoring/market-cap-calculator.ts:141` — `TOTAL_POSSIBLE_SIGNALS` (comment out const)
- `supabase/functions/sync-huggingface/index.ts:214` — `totalUpdated` (comment out assignment)

**Unused constants** (comment out with `// REMOVED` tag):
- `src/components/models/ranking-weight-controls.tsx:17` — `MIN_WEIGHT` const

**Unused function params** — these need care to preserve positional args:
- `src/app/api/admin/bootstrap/route.ts:13` — `_request`: already underscore-prefixed, keep as-is but if ESLint still flags it, the `no-unused-vars` rule config may need `argsIgnorePattern: "^_"`. Check if the rule is configured this way. If not, these params need inline `// eslint-disable-next-line @typescript-eslint/no-unused-vars` or the param kept with the underscore prefix.
- `src/components/marketplace/wallet-deposit-panel.tsx:17` — `_price`: same treatment
- `src/components/three/scene-content.tsx:97` — `delta`: prefix with underscore to become `_delta`
- `supabase/functions/sync-huggingface/index.ts:197` — `req`: prefix with underscore to become `_req`

**Test file unused params** — CRITICAL: do NOT remove these params, only prefix with underscore if not already:
- `src/components/layout/market-ticker.test.tsx:10` — `children` mock param: prefix with `_`
- `src/components/models/ranking-weight-controls.test.tsx:22` — `asChild` mock param: prefix with `_`
- `src/lib/compute-scores/compute-all-lenses.test.ts:147` — `thenable`: prefix with `_` or comment out if truly unused
- `src/lib/compute-scores/compute-all-lenses.test.ts:163` — `_args`: already prefixed
- `src/lib/compute-scores/compute-all-lenses.test.ts:170` — `_table`: already prefixed
- `src/lib/compute-scores/fetch-inputs.test.ts:44` — `_args`: already prefixed
- `src/lib/compute-scores/persist-results.test.ts:117` — `_data`: already prefixed
- `src/lib/compute-scores/persist-results.test.ts:130` — `_data`, `_opts`: already prefixed

For already-underscore-prefixed params that ESLint STILL flags: check `eslint.config.mjs` for `argsIgnorePattern`. If missing, add `argsIgnorePattern: "^_"` to the `no-unused-vars` rule, OR add inline eslint-disable comments on each line.

After all changes, run `npx tsc --noEmit` and `npx vitest run` to verify no regressions.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=dot</automated>
  </verify>
  <done>All ~40 unused-import/variable warnings resolved via `// REMOVED` tags or underscore prefixing. TypeScript compiles clean. All 222 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix accessibility and Next.js image warnings</name>
  <files>
    src/components/search-dialog.tsx,
    src/app/(marketplace)/marketplace/auctions/[id]/auction-detail-content.tsx
  </files>
  <action>
Fix the remaining 2 non-React-compiler warnings:

**1. Accessibility: `jsx-a11y/role-has-required-aria-props` in search-dialog.tsx:157**
The combobox role requires `aria-expanded` and `aria-controls` attributes. Read the element at line 157, then:
- Add `aria-expanded={open}` (or the boolean tracking whether results are shown)
- Add `aria-controls="search-results-listbox"` (or whatever ID the results list uses)
- Ensure the results list element has a matching `id` attribute
- If the results list uses `role="listbox"`, add a corresponding `id` prop

**2. Next.js image: `@next/next/no-img-element` in auction-detail-content.tsx:250**
Replace the `<img>` element with Next.js `<Image>` component:
- Add `import Image from "next/image"` at top of file
- Replace `<img src={...} alt={...} ... />` with `<Image src={...} alt={...} width={W} height={H} />` (determine appropriate width/height from the existing img styling/context)
- If the image URL is external, ensure the domain is in `next.config.ts` `images.remotePatterns` (or use `unoptimized` prop if it's a dynamic user-uploaded URL)

After both fixes, run the full verification suite.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=dot && npx eslint src/components/search-dialog.tsx src/app/(marketplace)/marketplace/auctions/[id]/auction-detail-content.tsx</automated>
  </verify>
  <done>Combobox has aria-expanded and aria-controls attributes. img element replaced with Next.js Image component. Both files pass ESLint with zero warnings.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` — zero errors
2. `npx vitest run` — all 222 tests pass
3. `npx eslint .` — Category A, C, D warnings all resolved (only React compiler warnings from Category B should remain)
</verification>

<success_criteria>
- All ~40 unused-import/variable warnings resolved with `// REMOVED` tags
- Accessibility aria-props warning fixed on search-dialog combobox
- Next.js no-img-element warning fixed on auction-detail-content
- `npx tsc --noEmit` passes clean
- All 222 vitest tests pass
- Only React compiler warnings (Category B) remain in ESLint output
</success_criteria>

<output>
After completion, create `.planning/phases/16-code-simplification/16-01-SUMMARY.md`
</output>
