---
phase: 09-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/instrumentation.ts
  - src/instrumentation-client.ts
  - sentry.server.config.ts
  - sentry.edge.config.ts
  - src/lib/api-error.ts
  - src/app/global-error.tsx
  - src/app/error.tsx
  - src/app/(admin)/error.tsx
  - src/app/(auth)/error.tsx
  - src/app/(auth)/orders/[id]/error.tsx
  - src/app/(catalog)/error.tsx
  - src/app/(marketplace)/dashboard/error.tsx
  - src/app/(marketplace)/dashboard/seller/error.tsx
  - src/app/(marketplace)/dashboard/seller/listings/[slug]/error.tsx
  - src/app/(marketplace)/error.tsx
  - src/app/(rankings)/error.tsx
  - src/app/(static)/about/error.tsx
  - src/app/(static)/error.tsx
  - src/app/(static)/privacy/error.tsx
  - next.config.ts
  - Dockerfile
autonomous: true
requirements: [OBS-01, OBS-02, OBS-05, PERF-03]

must_haves:
  truths:
    - "Unhandled 5xx errors in API routes are captured by Sentry via handleApiError"
    - "Client-side errors in error boundaries are captured by Sentry via captureException"
    - "Source maps are uploaded during Docker build so Sentry shows readable stack traces"
    - "CSP headers allow Sentry and PostHog domains without browser console violations"
    - "Bundle increase is controlled via tree-shaking (no tracing, no replay)"
  artifacts:
    - path: "src/instrumentation.ts"
      provides: "Sentry server/edge runtime registration"
      exports: ["register", "onRequestError"]
    - path: "src/instrumentation-client.ts"
      provides: "Sentry client-side initialization"
      contains: "Sentry.init"
    - path: "sentry.server.config.ts"
      provides: "Sentry server SDK config"
      contains: "Sentry.init"
    - path: "sentry.edge.config.ts"
      provides: "Sentry edge SDK config"
      contains: "Sentry.init"
    - path: "src/lib/api-error.ts"
      provides: "Centralized error handler with Sentry capture"
      contains: "Sentry.captureException"
    - path: "next.config.ts"
      provides: "withSentryConfig wrapper and CSP headers for Sentry+PostHog"
      contains: "withSentryConfig"
  key_links:
    - from: "src/lib/api-error.ts"
      to: "@sentry/nextjs"
      via: "Sentry.captureException for non-ApiError (5xx) errors"
      pattern: "Sentry\\.captureException"
    - from: "src/instrumentation.ts"
      to: "sentry.server.config.ts"
      via: "dynamic import in register()"
      pattern: "import.*sentry\\.server\\.config"
    - from: "next.config.ts"
      to: "@sentry/nextjs"
      via: "withSentryConfig wrapper"
      pattern: "withSentryConfig"
---

<objective>
Integrate Sentry error tracking into the application and prepare infrastructure for PostHog.

Purpose: Production errors currently go to console.error and are invisible. This plan adds Sentry to capture all unexpected server and client errors with readable stack traces, plus updates CSP headers and Dockerfile for both Sentry and PostHog.

Output: Sentry captures 5xx errors from all 68 API routes (via handleApiError) and client errors from all 13 error boundaries. Source maps upload during Docker build. CSP headers allow both Sentry and PostHog domains.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-observability/09-CONTEXT.md
@.planning/phases/09-observability/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/api-error.ts (MODIFY):
```typescript
import { systemLog } from "@/lib/logging";

export class ApiError extends Error {
  constructor(public statusCode: number, message: string) {
    super(message);
  }
}

export function handleApiError(error: unknown, source: string): Response {
  // Currently uses systemLog only — add Sentry.captureException for non-ApiError
}
```

From next.config.ts (MODIFY):
```typescript
// Currently: export default nextConfig
// After: export default withSentryConfig(nextConfig, { ... })
// CSP currently allows: 'self', supabase, clearbit
// Must add: *.ingest.sentry.io, us.i.posthog.com, us.posthog.com
```

From src/app/error.tsx (pattern for all 13 error.tsx files):
```typescript
useEffect(() => {
  console.error(error);  // Replace with Sentry.captureException(error)
}, [error]);
```

From Dockerfile Stage 2:
```dockerfile
# Currently: just RUN npm run build
# Must add: ARG SENTRY_AUTH_TOKEN + ENV before build step
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install SDKs and create Sentry config files</name>
  <files>
    package.json,
    src/instrumentation.ts,
    src/instrumentation-client.ts,
    sentry.server.config.ts,
    sentry.edge.config.ts
  </files>
  <action>
1. Run `npm install @sentry/nextjs posthog-js` (install both now to avoid a second install step in Plan 02).

2. Create `src/instrumentation.ts` with:
   - `register()` async function that dynamically imports `../../sentry.server.config` when NEXT_RUNTIME is "nodejs" and `../../sentry.edge.config` when NEXT_RUNTIME is "edge"
   - Export `onRequestError = Sentry.captureRequestError` from @sentry/nextjs
   - NOTE: The file lives in `src/` because the project uses `src/` directory structure. The imports to sentry configs at project root need `../../` relative paths.

3. Create `src/instrumentation-client.ts` with Sentry.init:
   - dsn: process.env.NEXT_PUBLIC_SENTRY_DSN
   - environment: process.env.NODE_ENV
   - tracesSampleRate: 0 (errors only, per user decision)
   - integrations: [] (no replay, no tracing)
   - replaysSessionSampleRate: 0
   - replaysOnErrorSampleRate: 0

4. Create `sentry.server.config.ts` at project root with Sentry.init:
   - dsn: process.env.NEXT_PUBLIC_SENTRY_DSN
   - environment: process.env.NODE_ENV
   - tracesSampleRate: 0
   - debug: false

5. Create `sentry.edge.config.ts` at project root with Sentry.init:
   - dsn: process.env.NEXT_PUBLIC_SENTRY_DSN
   - environment: process.env.NODE_ENV
   - tracesSampleRate: 0
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>@sentry/nextjs and posthog-js in package.json. Four Sentry config files created and typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument error handlers, update next.config.ts, CSP, Dockerfile, and all error boundaries</name>
  <files>
    src/lib/api-error.ts,
    next.config.ts,
    Dockerfile,
    src/app/global-error.tsx,
    src/app/error.tsx,
    src/app/(admin)/error.tsx,
    src/app/(auth)/error.tsx,
    src/app/(auth)/orders/[id]/error.tsx,
    src/app/(catalog)/error.tsx,
    src/app/(marketplace)/dashboard/error.tsx,
    src/app/(marketplace)/dashboard/seller/error.tsx,
    src/app/(marketplace)/dashboard/seller/listings/[slug]/error.tsx,
    src/app/(marketplace)/error.tsx,
    src/app/(rankings)/error.tsx,
    src/app/(static)/about/error.tsx,
    src/app/(static)/error.tsx,
    src/app/(static)/privacy/error.tsx
  </files>
  <action>
1. **handleApiError** (`src/lib/api-error.ts`): Add `import * as Sentry from "@sentry/nextjs"` at top. In the else branch (non-ApiError / 5xx errors), add `Sentry.captureException(error, { tags: { source } })` BEFORE the existing systemLog.error call. Do NOT capture ApiError instances (4xx are expected noise per user decision). Do NOT add request body or query params to Sentry context (PII risk per user decision).

2. **next.config.ts**:
   - Add `import { withSentryConfig } from "@sentry/nextjs"` at top.
   - Update CSP header value to add both Sentry AND PostHog domains:
     - connect-src: append `https://*.ingest.sentry.io https://us.i.posthog.com`
     - script-src: append `https://us.posthog.com` (needed for PostHog autocapture JS)
   - Change `export default nextConfig` to `export default withSentryConfig(nextConfig, { ... })` with:
     - org: process.env.SENTRY_ORG
     - project: process.env.SENTRY_PROJECT
     - authToken: process.env.SENTRY_AUTH_TOKEN
     - silent: !process.env.CI
     - sourcemaps.deleteSourcemapsAfterUpload: true
     - bundleSizeOptimizations.excludeTracing: true
     - bundleSizeOptimizations.excludeDebugStatements: true

3. **Dockerfile Stage 2**: Add `ARG SENTRY_AUTH_TOKEN` and `ENV SENTRY_AUTH_TOKEN=${SENTRY_AUTH_TOKEN}` BEFORE the `RUN npm run build` line. This ensures source maps are uploaded during Docker build when the token is provided as a build arg in Coolify.

4. **global-error.tsx**: Add `import * as Sentry from "@sentry/nextjs"` and `import { useEffect } from "react"`. Add `useEffect(() => { Sentry.captureException(error); }, [error])` inside the component before the return statement. Keep existing inline styles (global-error cannot use Tailwind since it replaces the entire html/body).

5. **All 13 error.tsx files**: In each file, add `import * as Sentry from "@sentry/nextjs"`. Replace `console.error(error)` with `Sentry.captureException(error)` inside the existing useEffect. Keep all existing UI markup unchanged.

The 13 error.tsx files all follow the same pattern: they have `useEffect(() => { console.error(error); }, [error])`. Simply swap `console.error(error)` for `Sentry.captureException(error)` and add the Sentry import.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>handleApiError sends 5xx errors to Sentry with source tag. All 13 error.tsx files + global-error.tsx capture exceptions via Sentry. next.config.ts wrapped with withSentryConfig including source map upload and tree-shaking. CSP allows Sentry + PostHog domains. Dockerfile passes SENTRY_AUTH_TOKEN as build arg. All existing tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes clean
- `npx vitest run` — all existing tests pass (170+)
- `grep -r "Sentry.captureException" src/app/ src/lib/api-error.ts` shows 14 hits (13 error.tsx + 1 api-error.ts) plus global-error.tsx
- `grep "withSentryConfig" next.config.ts` confirms wrapper
- `grep "ingest.sentry.io" next.config.ts` confirms CSP update
- `grep "us.i.posthog.com" next.config.ts` confirms PostHog CSP pre-configured
- `grep "SENTRY_AUTH_TOKEN" Dockerfile` confirms build arg
</verification>

<success_criteria>
- Sentry SDK initializes on server, edge, and client runtimes
- handleApiError captures 5xx errors to Sentry, ignores 4xx
- All 13 error boundaries + global-error capture client exceptions
- Source maps configured for upload during Docker build
- CSP headers allow both Sentry and PostHog domains
- Tree-shaking excludes tracing and debug statements
- All existing tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/09-observability/09-01-SUMMARY.md`
</output>
