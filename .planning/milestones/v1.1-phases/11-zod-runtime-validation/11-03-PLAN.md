---
phase: 11-zod-runtime-validation
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/models/comments-section.tsx
  - src/components/marketplace/listing-reviews.tsx
  - src/components/marketplace/seller-orders-table.tsx
  - src/components/marketplace/seller-listings-table.tsx
  - src/app/(auth)/orders/orders-content.tsx
  - src/app/(auth)/orders/[id]/order-detail-content.tsx
  - src/app/(auth)/profile/profile-content.tsx
  - src/components/charts/quality-price-frontier.tsx
  - src/components/charts/rank-timeline.tsx
autonomous: true
requirements: [TYPE-01]

must_haves:
  truths:
    - "All client component Supabase casts use parseQueryResult instead of as unknown as"
    - "Recharts component prop casts are fixed with proper typing (not parseQueryResult)"
    - "No as unknown as casts remain in production code (only test file mock casts)"
    - "npx tsc --noEmit passes clean"
    - "All 170+ existing tests still pass"
  artifacts:
    - path: "src/components/models/comments-section.tsx"
      provides: "Comments with validated Supabase queries"
      contains: "parseQueryResult"
    - path: "src/components/marketplace/listing-reviews.tsx"
      provides: "Reviews with validated Supabase queries"
      contains: "parseQueryResult"
    - path: "src/components/marketplace/seller-orders-table.tsx"
      provides: "Seller orders with validated Supabase queries"
      contains: "parseQueryResult"
    - path: "src/components/charts/quality-price-frontier.tsx"
      provides: "Chart with properly typed Recharts callback"
    - path: "src/components/charts/rank-timeline.tsx"
      provides: "Chart with properly typed Recharts payload"
  key_links:
    - from: "Client components"
      to: "src/lib/schemas/parse.ts"
      via: "import { parseQueryResult }"
      pattern: "import.*parseQueryResult.*from.*@/lib/schemas"
    - from: "src/lib/schemas/parse.ts"
      to: "Sentry (client-side)"
      via: "reportSchemaError with try-catch fallback to console.error"
      pattern: "console\\.error.*schema-validation"
---

<objective>
Replace all remaining `as unknown as` casts in client components and fix Recharts prop casts with proper typing.

Purpose: This plan completes the cast migration by handling the 7 client component files (which fetch data via Supabase browser client) and 2 Recharts chart components with UI library boundary casts. After this plan, the only `as unknown as` casts remaining will be the 3 test file mock casts (intentionally left as-is).

Output: Zero production `as unknown as` casts. All client components use parseQueryResult. Recharts casts replaced with proper typing. TypeScript clean. All tests pass.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-zod-runtime-validation/11-CONTEXT.md
@.planning/phases/11-zod-runtime-validation/11-RESEARCH.md
@.planning/phases/11-zod-runtime-validation/11-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/lib/schemas/parse.ts:
```typescript
export function parseQueryResult<T>(
  response: SupabaseResponse,
  schema: z.ZodType<T>,
  schemaName: string,
): T[];

export function parseQueryResultSingle<T>(
  response: SupabaseResponse,
  schema: z.ZodType<T>,
  schemaName: string,
): T | null;

// reportSchemaError has try-catch: if Sentry not available on client,
// falls back to console.error("[schema-validation] ...", issues)
```

From src/lib/schemas/community.ts:
```typescript
export const CommentSchema: z.ZodType<Comment>;
export const ProfileSchema: z.ZodType<Profile>;
export const BookmarkSchema: z.ZodType<BookmarkWithModel>;
export const WatchlistSchema: z.ZodType<Watchlist>;
```

From src/lib/schemas/marketplace.ts:
```typescript
export const MarketplaceListingSchema: z.ZodType<MarketplaceListing>;
export const MarketplaceReviewSchema: z.ZodType<Review>;
export const MarketplaceOrderSchema: z.ZodType<Order>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate client component Supabase casts (7 files, ~10 casts)</name>
  <files>
    src/components/models/comments-section.tsx,
    src/components/marketplace/listing-reviews.tsx,
    src/components/marketplace/seller-orders-table.tsx,
    src/components/marketplace/seller-listings-table.tsx,
    src/app/(auth)/orders/orders-content.tsx,
    src/app/(auth)/orders/[id]/order-detail-content.tsx,
    src/app/(auth)/profile/profile-content.tsx
  </files>
  <action>
    Apply the same transformation pattern as Plan 02 to all client component files. These are `"use client"` components that call Supabase via the browser client.

    **Important client-side note:** `parseQueryResult` calls `Sentry.captureException` internally. The `reportSchemaError` function wraps this in a try-catch, so if Sentry is not initialized on the client side, it falls back to `console.error`. No special handling needed in the consuming code.

    **Per-file migration:**

    - `src/components/models/comments-section.tsx` (2 casts):
      - Line ~58: `rawData as unknown as Comment[]` -- replace with `parseQueryResult(response, CommentSchema, "Comment")`. Change `const { data: rawData } = await supabase...` to `const response = await supabase...`.
      - Line ~87: `replyData as unknown as Comment[]` -- same pattern for reply fetch.

    - `src/components/marketplace/listing-reviews.tsx` (2 casts):
      - Line ~96: `(rawData ?? []) as unknown as ReviewWithProfile[]` -- replace with `parseQueryResult(response, ReviewWithProfileSchema, "ReviewWithProfile")`.
      - Line ~105: `(profilesRaw ?? []) as unknown as ReviewProfileRow[]` -- replace with `parseQueryResult(profilesResponse, ProfileSchema.pick({...}), "ReviewProfile")` or a dedicated ReviewProfileSchema.

    - `src/components/marketplace/seller-orders-table.tsx` (2 casts):
      - Line ~57: `(rawData ?? []) as unknown as SellerOrderRow[]` -- replace with `parseQueryResult(response, SellerOrderSchema, "SellerOrder")`.
      - Line ~66: `(profilesRaw ?? []) as unknown as OrderProfileRow[]` -- replace with `parseQueryResult(profilesResponse, ProfileSchema.pick({...}), "OrderProfile")` or a dedicated schema.

    - `src/components/marketplace/seller-listings-table.tsx` (1 cast):
      - Line ~58: `(data || []) as unknown as MarketplaceListing[]` -- replace with `parseQueryResult(response, MarketplaceListingSchema, "MarketplaceListing")`.

    - `src/app/(auth)/orders/orders-content.tsx` (1 cast):
      - Line ~62: `(data as unknown as OrderWithJoins[]) ?? []` -- replace with `parseQueryResult(response, OrderWithJoinsSchema, "OrderWithJoins")`.

    - `src/app/(auth)/orders/[id]/order-detail-content.tsx` (1 cast):
      - Line ~80: `rawData as unknown as OrderWithParties | null` -- replace with `parseQueryResultSingle(response, OrderWithPartiesSchema, "OrderWithParties")`.

    - `src/app/(auth)/profile/profile-content.tsx` (1 cast):
      - Line ~67: `(rawData ?? []) as unknown as BookmarkWithModel[]` -- replace with `parseQueryResult(response, BookmarkWithModelSchema, "BookmarkWithModel")`.

    For any query-specific shapes not already in the schema files (e.g., ReviewWithProfile, SellerOrderRow, OrderWithJoins, OrderWithParties, BookmarkWithModel), add them to the appropriate schema file using `.pick()` / `.extend()` from the base schema. Check the inline type definitions in each file to determine the exact shape needed.

    After ALL replacements, run `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run -x</automated>
  </verify>
  <done>All 7 client component files have casts replaced with parseQueryResult calls. Any new query-specific schemas added to the schema files. TypeScript compiles clean. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Recharts casts with proper typing and verify zero production casts</name>
  <files>
    src/components/charts/quality-price-frontier.tsx,
    src/components/charts/rank-timeline.tsx
  </files>
  <action>
    **Recharts casts (2 casts -- NOT parseQueryResult, these are UI library boundary casts):**

    - `src/components/charts/quality-price-frontier.tsx:393`: `_data as unknown as { payload: QualityPriceDataPoint } | QualityPriceDataPoint`
      - This is a Recharts callback parameter. Fix by properly typing the callback function parameter. Check what Recharts exports for this callback type (e.g., `CategoricalChartState` or the specific callback signature). If Recharts does not export a usable type, use a properly annotated type assertion with a comment explaining why, or type the parameter as the Recharts-provided type and extract the payload field.

    - `src/components/charts/rank-timeline.tsx:456`: `payload as unknown as Array<{ color: string; name: string; value: number }>`
      - This is a Recharts tooltip payload cast. Fix by importing the Recharts `Payload` type from `recharts/types/component/DefaultTooltipContent` or by typing the CustomTooltip component's props using Recharts' `TooltipProps` generic. Pattern: `const CustomTooltip = ({ payload }: TooltipProps<number, string>) => { ... }`.

    **For both:** If Recharts types are insufficient and a type assertion is truly unavoidable, change from `as unknown as` to a single `as` assertion (which is less unsafe) with a `// Recharts callback typing limitation` comment. The goal is to eliminate `as unknown as` double-casts, not necessarily all type assertions.

    **Final verification -- confirm zero production casts remain:**

    Run: `grep -r "as unknown as" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".test."`

    Expected result: ONLY the 3 test file casts should remain:
    - `src/lib/compute-scores/fetch-inputs.test.ts` -- `as unknown as SupabaseClient`
    - `src/lib/compute-scores/persist-results.test.ts` -- `as unknown as SupabaseClient`
    - `src/lib/compute-scores/compute-all-lenses.test.ts` -- `as unknown as SupabaseClient`

    If any other casts remain, fix them before completing this task.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run -x</automated>
  </verify>
  <done>Recharts casts are fixed with proper typing or single-assertion. grep confirms only 3 test file mock casts remain. TypeScript compiles clean. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- no type errors
- `npx vitest run` -- all tests pass
- `grep -r "as unknown as" src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".test."` -- returns empty (zero production casts)
- Only 3 test file mock casts remain (intentional -- these cast mock objects to SupabaseClient for test setup)
</verification>

<success_criteria>
1. All 7 client component files migrated to parseQueryResult
2. Both Recharts casts fixed with proper typing
3. Zero `as unknown as` casts in production code (only 3 test mock casts remain)
4. npx tsc --noEmit passes clean
5. npx vitest run passes (all 170+ tests)
6. Phase 11 goal fully achieved: all unsafe casts replaced with Zod validation
</success_criteria>

<output>
After completion, create `.planning/phases/11-zod-runtime-validation/11-03-SUMMARY.md`
</output>
