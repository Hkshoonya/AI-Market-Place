---
phase: 06-type-safety
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/lib/data-sources/adapters/artificial-analysis.ts
  - src/lib/data-sources/adapters/bigcode-leaderboard.ts
  - src/lib/data-sources/adapters/chatbot-arena.ts
  - src/lib/data-sources/adapters/gaia-benchmark.ts
  - src/lib/data-sources/adapters/github-stars.ts
  - src/lib/data-sources/adapters/github-trending.ts
  - src/lib/data-sources/adapters/livebench.ts
  - src/lib/data-sources/adapters/open-llm-leaderboard.ts
  - src/lib/data-sources/adapters/open-vlm-leaderboard.ts
  - src/lib/data-sources/adapters/openrouter-models.ts
  - src/lib/data-sources/adapters/osworld.ts
  - src/lib/data-sources/adapters/seal-leaderboard.ts
  - src/lib/data-sources/adapters/tau-bench.ts
  - src/lib/data-sources/adapters/terminal-bench.ts
  - src/lib/data-sources/adapters/webarena.ts
  - src/lib/data-sources/orchestrator.ts
  - src/lib/data-sources/utils.ts
  - src/lib/agents/auth.ts
  - src/lib/agents/chat.ts
  - src/lib/agents/logger.ts
  - src/lib/agents/runtime.ts
  - src/lib/agents/utils.ts
  - src/lib/agents/residents/code-quality.ts
  - src/lib/agents/residents/pipeline-engineer.ts
  - src/lib/agents/residents/ux-monitor.ts
  - src/lib/marketplace/delivery.ts
  - src/lib/marketplace/escrow.ts
  - src/lib/marketplace/auctions/english.ts
  - src/lib/marketplace/auctions/dutch.ts
  - src/lib/cron-tracker.ts
  - src/lib/payments/wallet.ts
  - src/lib/mcp/tools.ts
  - src/lib/mcp/resources.ts
  - src/lib/mcp/prompts.ts
  - src/lib/logging.ts
autonomous: true
requirements: [TYPE-02, TYPE-05]

must_haves:
  truths:
    - "No src/lib/ file contains supabase as any or ctx.supabase as any"
    - "All adapter sync functions use ctx.supabase directly without casting"
    - "All agent context functions use ctx.supabase directly without casting"
  artifacts:
    - path: "src/lib/data-sources/adapters/*.ts"
      provides: "Type-safe adapter sync functions"
      min_lines: 10
    - path: "src/lib/agents/*.ts"
      provides: "Type-safe agent functions"
      min_lines: 10
  key_links:
    - from: "src/lib/data-sources/types.ts"
      to: "src/lib/data-sources/adapters/*.ts"
      via: "SyncContext.supabase typed as TypedSupabaseClient"
      pattern: "ctx\\.supabase"
    - from: "src/lib/agents/types.ts"
      to: "src/lib/agents/*.ts"
      via: "AgentContext.supabase typed as TypedSupabaseClient"
      pattern: "supabase"
---

<objective>
Remove all `supabase as any` casts from src/lib/ files.

Purpose: Plan 06-01 typed the `SyncContext.supabase` and `AgentContext.supabase` fields as `TypedSupabaseClient` and added Relationships to all Database tables. Now the `(supabase as any)` and `(ctx.supabase as any)` casts in src/lib/ files are unnecessary and must be removed. This addresses TYPE-02 (Supabase join operations use typed interfaces) and contributes to TYPE-05 (total any count reduction).

Output: All 35 src/lib/ files use properly typed Supabase client without any casts.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-type-safety/06-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, SyncContext.supabase is TypedSupabaseClient and all Database tables have Relationships. -->

Adapter pattern (all 15 adapters follow this):
```typescript
// BEFORE:
const sb = ctx.supabase as any;
sb.from("models").upsert(...)

// AFTER:
const sb = ctx.supabase;
sb.from("models").upsert(...)
```

Agent pattern (6 agent files):
```typescript
// BEFORE:
const supabase = ctx.supabase as any;

// AFTER:
const supabase = ctx.supabase;
```

Marketplace/lib pattern:
```typescript
// BEFORE (delivery.ts, escrow.ts, etc.):
const supabase = createAdminClient() as any;
// or
(supabase as any).from(...)

// AFTER:
const supabase = createAdminClient();
supabase.from(...)
```

For createAdminClient(), check src/lib/supabase/admin.ts -- it already returns SupabaseClient<Database>, so the cast is unnecessary.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove supabase-as-any from data adapters + data-sources utilities</name>
  <files>src/lib/data-sources/adapters/artificial-analysis.ts, src/lib/data-sources/adapters/bigcode-leaderboard.ts, src/lib/data-sources/adapters/chatbot-arena.ts, src/lib/data-sources/adapters/gaia-benchmark.ts, src/lib/data-sources/adapters/github-stars.ts, src/lib/data-sources/adapters/github-trending.ts, src/lib/data-sources/adapters/livebench.ts, src/lib/data-sources/adapters/open-llm-leaderboard.ts, src/lib/data-sources/adapters/open-vlm-leaderboard.ts, src/lib/data-sources/adapters/openrouter-models.ts, src/lib/data-sources/adapters/osworld.ts, src/lib/data-sources/adapters/seal-leaderboard.ts, src/lib/data-sources/adapters/tau-bench.ts, src/lib/data-sources/adapters/terminal-bench.ts, src/lib/data-sources/adapters/webarena.ts, src/lib/data-sources/orchestrator.ts, src/lib/data-sources/utils.ts</files>
  <action>
For each of the 15 adapter files in src/lib/data-sources/adapters/:
1. Find `const sb = ctx.supabase as any;` or `const supabase = ctx.supabase as any;`
2. Remove the `as any` cast: `const sb = ctx.supabase;` or `const supabase = ctx.supabase;`
3. Remove any `// eslint-disable-next-line @typescript-eslint/no-explicit-any` comment above the line
4. If the file has any `(data as any[])` or `.map((x: any) => ...)` patterns on Supabase query results, remove the `: any` annotation (let TypeScript infer the type from the now-typed query)

For src/lib/data-sources/orchestrator.ts (2 casts):
- Same pattern: remove `as any` from supabase client references
- Remove associated eslint-disable comments

For src/lib/data-sources/utils.ts (1 cast):
- Same pattern: remove `as any` from supabase client reference
- Remove associated eslint-disable comment

After each file edit, the query chain (`.from("table").select(...).upsert(...)`) should compile because the Database type now has all table definitions with Relationships.

If any query uses `.select("*, related_table(col1, col2)")` join syntax and TypeScript still complains about the result type, add an explicit result type annotation on the destructured `data` variable using the existing interfaces from src/types/database.ts. For example:
```typescript
const { data } = await sb.from("models").select("id, name");
// data is now typed as Pick<Model, "id" | "name">[] | null
```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Zero `supabase as any` or `ctx.supabase as any` casts remain in src/lib/data-sources/. All 17 files compile clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove supabase-as-any from agents, marketplace, payments, MCP, and other lib files</name>
  <files>src/lib/agents/auth.ts, src/lib/agents/chat.ts, src/lib/agents/logger.ts, src/lib/agents/runtime.ts, src/lib/agents/utils.ts, src/lib/agents/residents/code-quality.ts, src/lib/agents/residents/pipeline-engineer.ts, src/lib/agents/residents/ux-monitor.ts, src/lib/marketplace/delivery.ts, src/lib/marketplace/escrow.ts, src/lib/marketplace/auctions/english.ts, src/lib/marketplace/auctions/dutch.ts, src/lib/cron-tracker.ts, src/lib/payments/wallet.ts, src/lib/mcp/tools.ts, src/lib/mcp/resources.ts, src/lib/mcp/prompts.ts, src/lib/logging.ts</files>
  <action>
**Agent files (8 files):**
For src/lib/agents/auth.ts, chat.ts, logger.ts, runtime.ts, utils.ts, and residents/code-quality.ts, pipeline-engineer.ts, ux-monitor.ts:
1. Find all `(supabase as any)` or `(ctx.supabase as any)` patterns
2. Remove the `as any` cast
3. Remove associated eslint-disable comments
4. Remove `: any` from any `.map()` callbacks on Supabase query results

In src/lib/agents/utils.ts (5 casts) and src/lib/agents/chat.ts (4 casts), there are multiple casts per file. Remove all of them.

**Marketplace files (4 files):**
For src/lib/marketplace/delivery.ts (6 casts), escrow.ts (2 casts), auctions/english.ts (2 casts), auctions/dutch.ts (1 cast):
1. These use `createAdminClient()` which already returns `SupabaseClient<Database>`
2. Remove `as any` casts from `createAdminClient() as any` or `(supabase as any).from(...)`
3. Remove associated eslint-disable comments

**Other lib files (6 files):**
For src/lib/cron-tracker.ts (3 casts), src/lib/payments/wallet.ts (1 cast), src/lib/mcp/tools.ts (1 cast), src/lib/mcp/resources.ts (1 cast), src/lib/mcp/prompts.ts (1 cast), src/lib/logging.ts (1 cast):
1. Remove `as any` casts
2. Remove associated eslint-disable comments

If TypeScript errors arise from any query result type mismatch after removing `as any`, add a typed annotation. For MCP files that may pass supabase client from an external source, check if the parameter type needs updating to accept `TypedSupabaseClient`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Zero `supabase as any` casts remain in any src/lib/ file. All 18 files compile clean. Total src/lib/ supabase-as-any count is 0.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes clean
2. `grep -rn "supabase as any" src/lib/ --include="*.ts" --include="*.tsx" | wc -l` returns 0
3. `grep -rn "eslint-disable.*no-explicit-any" src/lib/ --include="*.ts" --include="*.tsx" | wc -l` is significantly reduced
</verification>

<success_criteria>
- Zero `supabase as any` casts in src/lib/
- All adapter, agent, marketplace, and utility files use typed Supabase client
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-type-safety/06-02-SUMMARY.md`
</output>
