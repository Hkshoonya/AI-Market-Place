---
phase: 06-type-safety
plan: 05
type: execute
wave: 3
depends_on: [06-02, 06-03, 06-04]
files_modified: []
autonomous: true
requirements: [TYPE-05]

must_haves:
  truths:
    - "grep for any across src/ reports under 20 occurrences"
    - "npx tsc --noEmit passes clean"
    - "No supabase as any casts exist anywhere in the codebase"
    - "No catch (err: any) blocks exist anywhere in the codebase"
  artifacts: []
  key_links: []
---

<objective>
Final any count verification and remaining cleanup to reach under-20 target.

Purpose: After Plans 01-04 fixed the Database type, context interfaces, catch blocks, supabase casts, compare-client types, and admin enrichment types, this plan verifies the total `any` count is under 20 (TYPE-05). If stragglers remain above the threshold, this plan identifies and fixes them. Files touched will be determined at execution time based on what remains.

Output: Verified `any` count under 20. Phase 6 complete.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-type-safety/06-01-SUMMARY.md
@.planning/phases/06-type-safety/06-02-SUMMARY.md
@.planning/phases/06-type-safety/06-03-SUMMARY.md
@.planning/phases/06-type-safety/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit remaining any occurrences and fix stragglers</name>
  <files>Determined at execution time</files>
  <action>
**Step 1: Count remaining any occurrences.**

Run: `grep -rn ": any\b\|as any\|<any>" src/ --include="*.ts" --include="*.tsx" | grep -v "//.*any" | grep -v node_modules | wc -l`

Also run the specific verification from the roadmap:
`grep -r "any" src/ --include="*.ts" --include="*.tsx" | grep -v "//.*any" | wc -l`

**Step 2: If count > 20, identify remaining files:**

`grep -rn ": any\b\|as any\|<any>" src/ --include="*.ts" --include="*.tsx" | grep -v "//.*any" | grep -v node_modules | sed 's/:.*$//' | sort | uniq -c | sort -rn`

**Step 3: For each remaining file with `any`:**

Categories of acceptable remaining `any` (do NOT fix these):
- Framework-required `any` (e.g., React error boundaries, third-party library callbacks that genuinely need `any`)
- Generated code that should not be modified
- Legitimate `any` in type utility functions

Categories that MUST be fixed:
- `supabase as any` -- should be zero after Plans 02-04
- `catch (err: any)` -- should be zero after Plan 01
- `data as any[]` -- replace with proper types
- `.map((x: any) => ...)` -- remove `: any` or add proper type
- `variable: any` -- replace with proper type
- `as any` type assertions -- replace with proper type or `as unknown as TargetType` if genuinely needed

**Step 4: Fix remaining files to get under 20.**

For each file, apply the minimal fix needed. Prefer:
1. Removing unnecessary `: any` annotations (let TypeScript infer)
2. Adding proper type imports from `@/types/database`
3. Defining local interfaces for complex shapes
4. Using `unknown` with type guards instead of `any`
5. As last resort: `// eslint-disable-next-line @typescript-eslint/no-explicit-any` with a comment explaining WHY

**Step 5: Remove orphaned eslint-disable comments.**

After all fixes, search for `eslint-disable.*no-explicit-any` comments that no longer guard an `any` usage (the `any` was removed but the comment wasn't). Remove these orphaned comments.

**Step 6: Final verification.**

Run `npx tsc --noEmit` to confirm the build is clean.
Run the count check again to confirm under 20.

Also verify specific requirement checks:
- TYPE-01: `grep -rn "catch.*: any" src/` returns 0
- TYPE-02: `grep -rn "supabase as any" src/` returns 0
- TYPE-03: `grep -c ": any" src/app/compare/compare-client.tsx` returns 0
- TYPE-04: `grep -rn "as any" src/app/\(admin\)/ --include="*.tsx"` returns 0 or near 0
- TYPE-05: total any count under 20
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Total any count is under 20. All TYPE-01 through TYPE-05 requirements verified. npx tsc --noEmit passes clean. Phase 6 type safety is complete.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes clean
2. `grep -r "any" src/ --include="*.ts" --include="*.tsx" | grep -v "//.*any" | wc -l` reports under 20
3. `grep -rn "catch.*: any" src/ --include="*.ts" --include="*.tsx"` returns 0 results
4. `grep -rn "supabase as any" src/ --include="*.ts" --include="*.tsx"` returns 0 results
5. `grep -c ": any" src/app/compare/compare-client.tsx` returns 0
</verification>

<success_criteria>
- Total `any` count under 20 (TYPE-05)
- All 5 TYPE requirements verified passing
- TypeScript compiles clean
- Phase 6 complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-type-safety/06-05-SUMMARY.md`
</output>
