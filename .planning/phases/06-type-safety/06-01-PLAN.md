---
phase: 06-type-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/database.ts
  - src/lib/data-sources/types.ts
  - src/lib/agents/types.ts
  - src/app/(auth)/wallet/wallet-content.tsx
  - src/components/marketplace/dutch-bid-panel.tsx
  - src/components/marketplace/english-bid-panel.tsx
  - src/components/marketplace/purchase-button.tsx
autonomous: true
requirements: [TYPE-01]

must_haves:
  truths:
    - "Every table in Database.public.Tables has a Relationships property"
    - "SyncContext.supabase and AgentContext.supabase are typed as SupabaseClient<Database> instead of unknown"
    - "All catch blocks use catch (err: unknown) with explicit narrowing, not catch (err: any)"
  artifacts:
    - path: "src/types/database.ts"
      provides: "Complete Database type with Relationships on all 39 tables"
      contains: "Relationships"
    - path: "src/lib/data-sources/types.ts"
      provides: "Typed supabase field on SyncContext"
      contains: "SupabaseClient"
    - path: "src/lib/agents/types.ts"
      provides: "Typed supabase field on AgentContext"
      contains: "SupabaseClient"
  key_links:
    - from: "src/types/database.ts"
      to: "src/lib/supabase/server.ts"
      via: "Database generic parameter"
      pattern: "createServerClient<Database>"
    - from: "src/lib/data-sources/types.ts"
      to: "src/lib/data-sources/adapters/*.ts"
      via: "SyncContext.supabase type"
      pattern: "ctx\\.supabase"
---

<objective>
Fix the Database type foundation and catch block type safety.

Purpose: The Database type in src/types/database.ts defines 39 tables but only 6 have the `Relationships` property. Without `Relationships`, the Supabase SDK cannot infer join types from `.from()` calls, forcing 162 `(supabase as any)` casts across the codebase. Adding `Relationships: []` to all tables unlocks typed queries everywhere. Additionally, the `SyncContext.supabase` and `AgentContext.supabase` fields are typed as `unknown`, forcing adapters and agents to cast. Finally, all 5 `catch (err: any)` blocks must use `unknown`.

Output: Updated Database type with Relationships on all tables, typed context interfaces, and fixed catch blocks.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/database.ts
@src/lib/data-sources/types.ts
@src/lib/agents/types.ts

<interfaces>
<!-- The Database type currently has 39 table definitions under Database.public.Tables. -->
<!-- 33 of them are missing the Relationships property. -->
<!-- Tables WITH Relationships already: profiles, user_bookmarks, comments, user_ratings, watchlists, watchlist_items -->
<!-- Tables MISSING Relationships: models, benchmarks, benchmark_scores, model_pricing, elo_ratings, rankings, model_updates, tags, model_tags, providers, model_snapshots, marketplace_listings, marketplace_reviews, marketplace_orders, notifications, notification_preferences, data_sources, model_news, sync_jobs, agents, agent_tasks, agent_logs, api_keys, agent_conversations, agent_messages, wallets, wallet_transactions, escrow_holds, platform_fee_tiers, api_endpoint_pricing, deployment_platforms, model_deployments, model_descriptions -->

From src/lib/data-sources/types.ts:
```typescript
export interface SyncContext {
  supabase: unknown;  // CHANGE TO: SupabaseClient<Database>
  config: Record<string, unknown>;
  secrets: Record<string, string>;
  lastSyncAt: string | null;
  signal?: AbortSignal;
}
```

From src/lib/agents/types.ts (line ~108):
```typescript
  supabase: unknown;  // CHANGE TO: SupabaseClient<Database>
```

Catch blocks to fix (5 total):
- src/app/(auth)/wallet/wallet-content.tsx:106 — catch (err: any)
- src/app/(auth)/wallet/wallet-content.tsx:132 — catch (err: any)
- src/components/marketplace/dutch-bid-panel.tsx:63 — catch (err: any)
- src/components/marketplace/english-bid-panel.tsx:76 — catch (err: any)
- src/components/marketplace/purchase-button.tsx:122 — catch (err: any)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Relationships to all Database table definitions</name>
  <files>src/types/database.ts</files>
  <action>
Add `Relationships: [];` to every table definition in `Database.public.Tables` that is currently missing it. There are 33 tables missing the property. The format for each table entry is:

```typescript
table_name: {
  Row: TypeName;
  Insert: ...;
  Update: ...;
  Relationships: [];  // ADD THIS LINE
};
```

Tables that ALREADY have Relationships (skip these): profiles, user_bookmarks, comments, user_ratings, watchlists, watchlist_items.

Tables that NEED Relationships added (all 33): models, benchmarks, benchmark_scores, model_pricing, elo_ratings, rankings, model_updates, tags, model_tags, providers, model_snapshots, marketplace_listings, marketplace_reviews, marketplace_orders, notifications, notification_preferences, data_sources, model_news, sync_jobs, agents, agent_tasks, agent_logs, api_keys, agent_conversations, agent_messages, wallets, wallet_transactions, escrow_holds, platform_fee_tiers, api_endpoint_pricing, deployment_platforms, model_deployments, model_descriptions.

Keep empty arrays `[]` for now -- actual relationship definitions are not needed because the codebase uses string-based `.select()` patterns, not the relationship inference API. The SDK only requires the property to exist.

Also add the SupabaseClient type re-export at the bottom of the file for convenience:

```typescript
import type { SupabaseClient } from "@supabase/supabase-js";
export type TypedSupabaseClient = SupabaseClient<Database>;
```

This import goes at the TOP of the file (before the existing type definitions). The type alias goes at the bottom, after the Database interface closing brace.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All 39 tables in Database.public.Tables have Relationships property. TypedSupabaseClient type alias exported. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Type context interfaces + fix catch blocks</name>
  <files>src/lib/data-sources/types.ts, src/lib/agents/types.ts, src/app/(auth)/wallet/wallet-content.tsx, src/components/marketplace/dutch-bid-panel.tsx, src/components/marketplace/english-bid-panel.tsx, src/components/marketplace/purchase-button.tsx</files>
  <action>
**Part A: Fix SyncContext.supabase type**

In `src/lib/data-sources/types.ts`, change `supabase: unknown` to `supabase: TypedSupabaseClient`:
- Add import at top: `import type { TypedSupabaseClient } from "@/types/database";`
- Change line `supabase: unknown;` to `supabase: TypedSupabaseClient;`

**Part B: Fix AgentContext.supabase type**

In `src/lib/agents/types.ts`, change `supabase: unknown` to `supabase: TypedSupabaseClient`:
- Add import at top: `import type { TypedSupabaseClient } from "@/types/database";`
- Change line `supabase: unknown;` to `supabase: TypedSupabaseClient;`

**Part C: Fix all 5 catch (err: any) blocks**

In each file, change `catch (err: any)` to `catch (err: unknown)` and add type narrowing. The pattern is:

```typescript
// BEFORE:
catch (err: any) {
  setError(err.message || "Something went wrong");
}

// AFTER:
catch (err: unknown) {
  setError(err instanceof Error ? err.message : "Something went wrong");
}
```

Files and locations:
1. `src/app/(auth)/wallet/wallet-content.tsx` line 106 and line 132 -- both use `err.message`
2. `src/components/marketplace/dutch-bid-panel.tsx` line 63 -- uses `err.message`
3. `src/components/marketplace/english-bid-panel.tsx` line 76 -- uses `err.message`
4. `src/components/marketplace/purchase-button.tsx` line 122 -- uses `err.message`

For each, also remove the associated `// eslint-disable-next-line @typescript-eslint/no-explicit-any` comment if present above the catch line.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>SyncContext.supabase and AgentContext.supabase typed as TypedSupabaseClient. All 5 catch blocks use `unknown` with instanceof narrowing. No `catch (err: any)` exists in codebase. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes clean
2. `grep -rn "catch.*: any" src/ --include="*.ts" --include="*.tsx" | wc -l` returns 0
3. `grep -c "Relationships" src/types/database.ts` returns 39 (one per table)
4. `grep "supabase: unknown" src/lib/data-sources/types.ts src/lib/agents/types.ts | wc -l` returns 0
</verification>

<success_criteria>
- All 39 Database tables have Relationships property
- SyncContext and AgentContext have typed supabase fields
- Zero catch (err: any) blocks remain
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-type-safety/06-01-SUMMARY.md`
</output>
