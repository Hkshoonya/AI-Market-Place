---
phase: 14-swr-data-fetching
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/marketplace/listing-reviews.tsx
  - src/app/(admin)/admin/listings/[slug]/edit/page.tsx
  - src/app/(auth)/settings/_components/notification-prefs-card.tsx
  - src/components/models/model-actions.tsx
autonomous: true
gap_closure: true
requirements: [PERF-01]
must_haves:
  truths:
    - "listing-reviews.tsx uses useSWR with inline Supabase fetcher instead of useState+useCallback+useEffect"
    - "admin listings edit page uses useSWR with inline Supabase fetcher instead of useState+useEffect"
    - "notification-prefs-card.tsx uses useSWR with API route instead of useState+useCallback+useEffect+fetch"
    - "model-actions.tsx bookmark check uses useSWR with auth-gated null key instead of useEffect+createClient"
  artifacts:
    - path: "src/components/marketplace/listing-reviews.tsx"
      provides: "SWR-converted review fetching with two-query enrichment in inline fetcher"
      contains: "useSWR"
    - path: "src/app/(admin)/admin/listings/[slug]/edit/page.tsx"
      provides: "SWR-converted admin listing edit with inline Supabase fetcher"
      contains: "useSWR"
    - path: "src/app/(auth)/settings/_components/notification-prefs-card.tsx"
      provides: "SWR-converted notification preferences fetching"
      contains: "useSWR"
    - path: "src/components/models/model-actions.tsx"
      provides: "SWR-converted bookmark check with conditional key"
      contains: "useSWR"
  key_links:
    - from: "src/components/marketplace/listing-reviews.tsx"
      to: "src/lib/swr/config.ts"
      via: "import SWR_TIERS"
      pattern: "SWR_TIERS\\.MEDIUM"
    - from: "src/app/(admin)/admin/listings/[slug]/edit/page.tsx"
      to: "src/lib/swr/config.ts"
      via: "import SWR_TIERS"
      pattern: "SWR_TIERS\\.SLOW"
    - from: "src/app/(auth)/settings/_components/notification-prefs-card.tsx"
      to: "src/lib/swr/config.ts"
      via: "import SWR_TIERS"
      pattern: "SWR_TIERS\\.SLOW"
    - from: "src/components/models/model-actions.tsx"
      to: "src/lib/swr/config.ts"
      via: "import SWR_TIERS"
      pattern: "SWR_TIERS\\.SLOW"
---

<objective>
Convert the 4 remaining data-fetching components that were missed during Phase 14 Plans 01-05 to use SWR hooks, closing all verification gaps.

Purpose: Complete the "all client-side useState+useEffect+fetch patterns replaced" truth from the Phase 14 verification report. These 4 components fell through gaps in the original file inventory.
Output: 4 components fully converted to useSWR with appropriate tiers and patterns.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-swr-data-fetching/14-VERIFICATION.md

<interfaces>
<!-- SWR infrastructure established in Plan 01, used by all gap closure tasks -->

From src/lib/swr/config.ts:
```typescript
export const SWR_TIERS = {
  FAST: { refreshInterval: 30_000, dedupingInterval: 10_000 },
  MEDIUM: { refreshInterval: 60_000, dedupingInterval: 30_000 },
  SLOW: { refreshInterval: 0, dedupingInterval: 300_000 },
} as const;
```

From src/lib/swr/fetcher.ts:
```typescript
export async function jsonFetcher<T>(url: string): Promise<T>;
```

From src/lib/schemas/parse.ts:
```typescript
export function parseQueryResult<T>(response: { data: unknown; error: unknown }, schema: ZodType<T>, label: string): T[];
export function parseQueryResultSingle<T>(response: { data: unknown; error: unknown }, schema: ZodType<T>, label: string): T | null;
```

Established patterns from Plans 02-05:
- Inline fetcher with createClient() inside for Supabase-direct queries
- Auth-gated null key: `user ? 'supabase:entity' : null`
- Cache key convention: `supabase:entity-name` prefix for Supabase-direct
- mutate() after POST/PUT/DELETE for cache revalidation
- SWR_TIERS spread: `{ ...SWR_TIERS.MEDIUM }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert listing-reviews.tsx and admin listings edit page to SWR</name>
  <files>src/components/marketplace/listing-reviews.tsx, src/app/(admin)/admin/listings/[slug]/edit/page.tsx</files>
  <action>
**listing-reviews.tsx** (Supabase two-query enrichment -> SWR inline fetcher):
1. Add `import useSWR from 'swr'` and `import { SWR_TIERS } from '@/lib/swr/config'`
2. Replace the `fetchReviews` useCallback + useEffect block (lines 88-130) with a single useSWR call:
   - Cache key: `supabase:listing-reviews:${listingId}`
   - Inline async fetcher that contains the existing two-query enrichment logic (createClient, fetch reviews, fetch profiles, merge)
   - Tier: `{ ...SWR_TIERS.MEDIUM }` (reviews change moderately)
3. Destructure `{ data: reviews, error: fetchError, isLoading: loading, mutate }` from useSWR
4. Remove the `reviews` useState, `loading` useState, `fetchError` useState, `fetchReviews` useCallback, and the useEffect that calls fetchReviews
5. In `handleSubmit`, replace `await fetchReviews()` with `mutate()` after successful POST
6. Update render: use `reviews ?? []` for the map, `loading` for skeleton, `fetchError?.message` for error display
7. Remove `useCallback` from the import if no longer used

**admin/listings/[slug]/edit/page.tsx** (Supabase single query -> SWR inline fetcher):
1. Add `import useSWR from 'swr'` and `import { SWR_TIERS } from '@/lib/swr/config'`
2. Move `const supabase = createClient()` from component body (line 43) into the SWR fetcher
3. Replace the useEffect block (lines 64-96) with a useSWR call:
   - Cache key: `supabase:admin-edit-listing:${slug}`
   - Inline async fetcher: creates Supabase client, fetches listing by slug with .single(), parses with parseQueryResultSingle, returns the data
   - Tier: `{ ...SWR_TIERS.SLOW }` (admin edit loads once, rarely auto-refreshes)
4. Destructure `{ data: listing, error: loadError, isLoading: loading }` from useSWR
5. Remove the `listing` useState, `loading` useState, and the useState-based `error` for fetch errors (keep the form submission `error` state)
6. The form fields (title, description, etc.) still need local state for editing. Populate them from `listing` using a useEffect that runs when `listing` changes: `useEffect(() => { if (listing) { setTitle(listing.title); ... } }, [listing])`
7. Update loading/not-found checks to use `loading` and `!listing` from SWR
8. Remove `supabase` from the component body (it is now only inside the fetcher). The handleSubmit uses fetch() to an API route, not supabase directly, so no change needed there.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Both files use useSWR with inline Supabase fetchers. No useState+useEffect fetch pattern remains. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Convert notification-prefs-card.tsx and model-actions.tsx to SWR</name>
  <files>src/app/(auth)/settings/_components/notification-prefs-card.tsx, src/components/models/model-actions.tsx</files>
  <action>
**notification-prefs-card.tsx** (API route fetch -> SWR):
1. Add `import useSWR from 'swr'` and `import { SWR_TIERS } from '@/lib/swr/config'`
2. Replace the `fetchNotifPrefs` useCallback + useEffect (lines 75-91) with a useSWR call:
   - Cache key: `/api/notifications/preferences` (API route, uses global jsonFetcher)
   - Tier: `{ ...SWR_TIERS.SLOW }` (preferences rarely change externally)
3. Destructure `{ data: savedPrefs, isLoading: notifLoading }` from useSWR
4. Keep the local `notifPrefs` useState for form editing, but initialize from SWR data using useEffect: `useEffect(() => { if (savedPrefs?.data) setNotifPrefs(prev => ({ ...prev, ...savedPrefs.data })); }, [savedPrefs])`
5. Remove the `notifLoading` useState, `fetchNotifPrefs` useCallback, and the useEffect that calls it
6. Remove `useCallback` from the import if no longer used
7. The `saveNotifPrefs` function keeps its existing fetch POST logic -- no SWR mutation needed since saving is a user-triggered action and the local state is the source of truth

**model-actions.tsx** (Supabase bookmark check -> SWR with auth-gated key):
1. Add `import useSWR from 'swr'` and `import { SWR_TIERS } from '@/lib/swr/config'`
2. Move `const supabase = createClient()` from component body (line 49) into the SWR fetcher
3. Replace the useEffect bookmark check (lines 51-68) with a useSWR call:
   - Cache key: `user && modelId ? \`supabase:bookmark:${user.id}:${modelId}\` : null` (null key when no user or no modelId, falls back to localStorage)
   - Inline async fetcher: creates Supabase client, queries user_bookmarks for the specific user+model combo, returns `!!data` (boolean)
   - Tier: `{ ...SWR_TIERS.SLOW }` (bookmark status rarely changes externally)
4. Destructure `{ data: dbBookmarked, mutate: mutateBookmark }` from useSWR
5. Compute `isBookmarked` from either SWR data (when user is logged in) or localStorage (when not): use a useEffect that sets `isBookmarked` based on `dbBookmarked` when user exists, or `getLocalBookmarks().includes(modelSlug)` when not
6. In `handleBookmark`, after the Supabase insert/delete, call `mutateBookmark()` to revalidate the SWR cache instead of manually calling setIsBookmarked for the DB path. Keep manual `setIsBookmarked` for the localStorage fallback path.
7. Remove `supabase` from the component body
8. Remove `useEffect` import only if no longer used (the showToast timer useEffect still needs it)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both files use useSWR. notification-prefs-card uses SWR for initial load with API route. model-actions uses SWR with auth-gated null key for bookmark check. No useState+useEffect fetch pattern remains in any of the 4 gap files. TypeScript compiles clean. All tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full gap closure:

1. **No old patterns remain in gap files:**
   - `grep -n "useEffect" src/components/marketplace/listing-reviews.tsx` -- should show NO fetch-related useEffect
   - `grep -n "useEffect" src/app/(admin)/admin/listings/[slug]/edit/page.tsx` -- should only show form population useEffect (not data fetching)
   - `grep -n "useEffect" src/app/(auth)/settings/_components/notification-prefs-card.tsx` -- should only show SWR data sync useEffect (not data fetching)
   - `grep -n "useEffect" src/components/models/model-actions.tsx` -- should only show toast timer and bookmark state sync (not data fetching)

2. **SWR imports present in all 4 files:**
   - `grep -l "useSWR" src/components/marketplace/listing-reviews.tsx src/app/(admin)/admin/listings/[slug]/edit/page.tsx src/app/(auth)/settings/_components/notification-prefs-card.tsx src/components/models/model-actions.tsx` -- should list all 4

3. **Full test suite:** `npx vitest run` -- all tests pass
4. **TypeScript:** `npx tsc --noEmit` -- clean
</verification>

<success_criteria>
- All 4 verification gap components converted from useState+useEffect+fetch/Supabase to useSWR
- Each component uses appropriate SWR tier (MEDIUM for listing-reviews, SLOW for admin edit/notification prefs/bookmark check)
- Auth-gated components use null key pattern when user is absent
- Mutation actions (review submit) use mutate() for cache revalidation
- TypeScript compiles clean
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-swr-data-fetching/14-06-SUMMARY.md`
</output>
