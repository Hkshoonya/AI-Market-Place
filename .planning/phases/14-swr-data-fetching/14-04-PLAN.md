---
phase: 14-swr-data-fetching
plan: 04
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/app/(auth)/watchlists/watchlists-content.tsx
  - src/app/(auth)/watchlists/[id]/watchlist-detail-content.tsx
  - src/app/(auth)/settings/api-keys/api-keys-content.tsx
  - src/app/(marketplace)/dashboard/seller/seller-dashboard-content.tsx
  - src/app/(marketplace)/marketplace/auctions/[id]/auction-detail-content.tsx
  - src/app/(marketplace)/marketplace/auctions/auctions-browse-content.tsx
  - src/app/(admin)/admin/verifications/page.tsx
  - src/app/(admin)/admin/listings/page.tsx
  - src/app/(admin)/admin/agents/agents-content.tsx
  - src/app/(admin)/admin/data-sources/page.tsx
  - src/app/(auth)/orders/[id]/order-detail-content.tsx
autonomous: true
requirements:
  - PERF-01

must_haves:
  truths:
    - "Auth-gated components pass null SWR key when user is not authenticated"
    - "Admin pages use SWR for data fetching with MEDIUM/SLOW tiers"
    - "Order detail page uses MEDIUM tier for message polling"
    - "Components with mutations call mutate() after successful POST/PUT/DELETE"
    - "No component mixes old useState+useEffect+fetch with new SWR patterns"
  artifacts:
    - path: "src/app/(auth)/watchlists/watchlists-content.tsx"
      provides: "SWR-converted watchlists with mutation revalidation"
      contains: "useSWR"
    - path: "src/app/(admin)/admin/verifications/page.tsx"
      provides: "SWR-converted admin verifications"
      contains: "useSWR"
    - path: "src/app/(auth)/orders/[id]/order-detail-content.tsx"
      provides: "SWR-converted order detail with message polling"
      contains: "useSWR"
  key_links:
    - from: "src/app/(auth)/watchlists/watchlists-content.tsx"
      to: "src/lib/swr/config.ts"
      via: "MEDIUM tier import"
      pattern: "SWR_TIERS\\.MEDIUM"
    - from: "src/app/(admin)/admin/listings/page.tsx"
      to: "src/lib/swr/config.ts"
      via: "MEDIUM tier import"
      pattern: "SWR_TIERS\\.MEDIUM"
---

<objective>
Convert 11 auth-gated page components and admin pages from useState+useEffect+fetch to useSWR hooks with conditional fetching and mutate() revalidation.

Purpose: These page-level components complete the API-route-based data fetching migration. They involve auth-gated conditional fetching, mutation+revalidation patterns, and multi-fetch pages. Split from the hooks/widgets in Plan 03 to keep each plan within context budget.

Output: 11 page components using useSWR with proper auth gating and mutation revalidation. All client-side API route fetching now goes through SWR.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-swr-data-fetching/14-RESEARCH.md
@.planning/phases/14-swr-data-fetching/14-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From src/lib/swr/config.ts:
```typescript
export const SWR_TIERS = {
  FAST: { refreshInterval: 30_000, dedupingInterval: 10_000 },
  MEDIUM: { refreshInterval: 60_000, dedupingInterval: 30_000 },
  SLOW: { refreshInterval: 0, dedupingInterval: 300_000 },
} as const;
```

<!-- Auth-gated conditional fetching pattern -->
```typescript
const { user } = useAuth();
const { data } = useSWR(user ? '/api/protected-endpoint' : null, { ...SWR_TIERS.MEDIUM });
```

<!-- Mutation + revalidation pattern -->
```typescript
const { data, mutate } = useSWR('/api/resource');
const handleCreate = async () => {
  await fetch('/api/resource', { method: 'POST', body: JSON.stringify(payload) });
  mutate(); // revalidate the GET cache
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert auth-gated user pages</name>
  <files>src/app/(auth)/watchlists/watchlists-content.tsx, src/app/(auth)/watchlists/[id]/watchlist-detail-content.tsx, src/app/(auth)/settings/api-keys/api-keys-content.tsx, src/app/(auth)/orders/[id]/order-detail-content.tsx, src/app/(marketplace)/dashboard/seller/seller-dashboard-content.tsx</files>
  <action>
Convert 5 auth-gated user page components to SWR:

1. `watchlists-content.tsx` -- MEDIUM tier, has create/delete mutations
   - useSWR for GET, mutate() after POST/DELETE.

2. `watchlist-detail-content.tsx` -- MEDIUM tier, has add/remove model mutations
   - Dynamic key: `user ? \`/api/watchlists/${id}\` : null`
   - mutate() after mutations.

3. `api-keys-content.tsx` -- SLOW tier, has create/delete mutations
   - useSWR for GET `/api/api-keys`, mutate() after create/revoke.

4. `order-detail-content.tsx` -- MEDIUM tier (polling for messages)
   - useSWR with MEDIUM tier for message polling.
   - Auth-gated key.

5. `seller-dashboard-content.tsx` -- MEDIUM tier, multi-fetch
   - May fetch from both `/api/marketplace/seller/verify` and `/api/marketplace/seller/stats`.
   - Use two useSWR calls, both auth-gated.

**For ALL components:**
- Add `import useSWR from 'swr'` and `import { SWR_TIERS } from '@/lib/swr/config'`.
- Remove useState for data/loading/error. Keep UI state (filters, form inputs, etc.).
- Replace useEffect+fetch with useSWR.
- Use `isLoading` from SWR instead of custom loading state.
- For error display, use `error?.message` or a generic fallback.
- For mutations, keep existing fetch POST/PUT/DELETE and call `mutate()` after success.
  </action>
  <verify>
    <automated>cd "F:/BotProject/AI Market Cap" && npx tsc --noEmit --pretty false 2>&1 | tail -20</automated>
  </verify>
  <done>5 auth-gated user pages converted to SWR. Watchlists, API keys, order detail, and seller dashboard all use conditional null-key pattern. Mutations revalidate via mutate(). TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Convert marketplace and admin pages</name>
  <files>src/app/(marketplace)/marketplace/auctions/[id]/auction-detail-content.tsx, src/app/(marketplace)/marketplace/auctions/auctions-browse-content.tsx, src/app/(admin)/admin/verifications/page.tsx, src/app/(admin)/admin/listings/page.tsx, src/app/(admin)/admin/agents/agents-content.tsx, src/app/(admin)/admin/data-sources/page.tsx</files>
  <action>
Convert 6 marketplace and admin page components to SWR:

**Marketplace pages:**

1. `auction-detail-content.tsx` -- FAST tier (auction data is time-sensitive)
   - Dynamic key: `` `/api/marketplace/auctions/${id}` ``

2. `auctions-browse-content.tsx` -- MEDIUM tier
   - Has filter params. Build URL with query string for SWR key.

**Admin pages:**

3. `admin/verifications/page.tsx` -- MEDIUM tier, has approve/reject mutations
   - useSWR for GET, mutate() after mutations.

4. `admin/listings/page.tsx` -- MEDIUM tier, has pagination
   - Build URL with page/status params. SWR refetches on param change.

5. `admin/agents/agents-content.tsx` -- MEDIUM tier, multi-fetch
   - May fetch from 3 endpoints. Use multiple useSWR calls.

6. `admin/data-sources/page.tsx` -- SLOW tier
   - Simple GET, no mutations expected.

**For ALL components:**
- Add `import useSWR from 'swr'` and `import { SWR_TIERS } from '@/lib/swr/config'`.
- Remove useState for data/loading/error. Keep UI state (filters, pagination, selected items).
- Replace useEffect+fetch with useSWR.
- Use `isLoading` from SWR instead of custom loading state.
- For mutations, keep existing fetch POST/PUT/DELETE and call `mutate()` after success.
  </action>
  <verify>
    <automated>cd "F:/BotProject/AI Market Cap" && npx vitest run && npx tsc --noEmit --pretty false 2>&1 | tail -20</automated>
  </verify>
  <done>6 marketplace and admin pages converted to SWR. Auction detail uses FAST tier. Admin pages use MEDIUM/SLOW tiers. Pagination keys include page/filter params. All mutations revalidate via mutate(). TypeScript compiles clean. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes clean
- `npx vitest run` all tests pass
- `grep -rn "useSWR" src/app/(admin)/admin/` shows admin pages use SWR
- `grep -rn "useSWR" src/app/(auth)/` shows auth-gated pages use SWR
- No component has both old useState+useEffect+fetch AND useSWR (no mixed patterns)
</verification>

<success_criteria>
All 11 auth-gated and admin page components converted to SWR. Auth-gated components use null-key conditional fetching. Mutation flows call mutate() for cache revalidation. No component mixes old and new patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/14-swr-data-fetching/14-04-SUMMARY.md`
</output>
