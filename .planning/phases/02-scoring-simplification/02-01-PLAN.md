---
phase: 02-scoring-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/scoring/scoring-helpers.ts
  - src/lib/scoring/usage-calculator.ts
  - src/lib/scoring/expert-calculator.ts
  - src/lib/scoring/market-cap-calculator.ts
  - src/lib/scoring/capability-calculator.ts
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03]

must_haves:
  truths:
    - "A shared addSignal() helper exists and accumulates {name, score, weight} into a signals array"
    - "A shared logNormalizeSignal() helper exists and replaces all inline log-normalization math"
    - "A shared weightedBenchmarkAvg() helper exists using the canonical 28-entry BENCHMARK_IMPORTANCE"
    - "A shared normalizeElo() helper replaces all inline (elo - 800) / (1400 - 800) * 100 calculations"
    - "A shared computeRecencyScore() helper replaces all inline exponential decay calculations"
    - "Usage calculator has one code path for open and proprietary models — no duplicated signal blocks"
    - "Coverage penalty lookup tables are used in all calculators that apply penalties (no if/else chains)"
    - "npx tsc --noEmit passes clean"
  artifacts:
    - path: "src/lib/scoring/scoring-helpers.ts"
      provides: "Shared scoring helper functions and BENCHMARK_IMPORTANCE"
      exports: ["logNormalizeSignal", "addSignal", "weightedBenchmarkAvg", "normalizeElo", "computeRecencyScore", "BENCHMARK_IMPORTANCE"]
    - path: "src/lib/scoring/usage-calculator.ts"
      provides: "Unified usage calculator with single code path"
  key_links:
    - from: "src/lib/scoring/expert-calculator.ts"
      to: "src/lib/scoring/scoring-helpers.ts"
      via: "import { weightedBenchmarkAvg, logNormalizeSignal, normalizeElo, computeRecencyScore, BENCHMARK_IMPORTANCE }"
      pattern: "from.*scoring-helpers"
    - from: "src/lib/scoring/market-cap-calculator.ts"
      to: "src/lib/scoring/scoring-helpers.ts"
      via: "import { logNormalizeSignal }"
      pattern: "from.*scoring-helpers"
    - from: "src/lib/scoring/capability-calculator.ts"
      to: "src/lib/scoring/scoring-helpers.ts"
      via: "import { normalizeElo, computeRecencyScore }"
      pattern: "from.*scoring-helpers"
---

<objective>
Create the shared scoring helpers module and eliminate duplication across calculators. Unify the usage calculator's duplicated open/proprietary branches into a single code path.

Purpose: Establish the shared helper foundation (SCORE-01) that all calculators will use, consolidate coverage penalty usage (SCORE-03), and merge the usage calculator's duplicated branches (SCORE-02).
Output: New `scoring-helpers.ts` with 5 helpers + canonical BENCHMARK_IMPORTANCE; unified `usage-calculator.ts`; expert/market-cap/capability calculators wired to shared helpers.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scoring-simplification/02-CONTEXT.md

@src/lib/scoring/usage-calculator.ts
@src/lib/scoring/expert-calculator.ts
@src/lib/scoring/market-cap-calculator.ts
@src/lib/scoring/capability-calculator.ts
@src/lib/constants/scoring.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scoring-helpers.ts with shared helpers and BENCHMARK_IMPORTANCE</name>
  <files>src/lib/scoring/scoring-helpers.ts</files>
  <action>
Create `src/lib/scoring/scoring-helpers.ts` with these exports:

1. `BENCHMARK_IMPORTANCE: Record<string, number>` — the canonical 28-entry list from quality-calculator.ts lines 106-140. This is the single source of truth; expert-calculator's 18-entry subset is eliminated.

2. `logNormalizeSignal(value: number, max: number): number` — extracted from usage-calculator.ts:47 / expert-calculator.ts:53. Identical implementation: `if (value <= 0 || max <= 0) return 0; return Math.min((Math.log10(value + 1) / Math.log10(max + 1)) * 100, 100);`

3. `addSignal(signals: Array<{name: string; score: number; weight: number}>, name: string, score: number, weight: number): void` — pushes `{name, score, weight}` onto the array. Simple helper that standardizes the signal accumulation pattern used in all calculators.

4. `weightedBenchmarkAvg(scores: Array<{slug: string; score: number}>): number` — extracted from quality-calculator.ts:142-154. Uses BENCHMARK_IMPORTANCE for weights, normalizes slugs with `.toLowerCase().replace(/_/g, "-")`. Falls back to importance 1.0 for unknown slugs.

5. `normalizeElo(eloScore: number): number` — extracted from the pattern at quality-calculator.ts:233, expert-calculator.ts:90, capability-calculator.ts:113. Implementation: `Math.min(Math.max((eloScore - 800) / (1400 - 800) * 100, 0), 100)`.

6. `computeRecencyScore(releaseDate: string | null, options?: { halfLifeMonths?: number; floor?: number }): number` — extracted from quality-calculator.ts:248, expert-calculator.ts:112, capability-calculator.ts:119. Default halfLifeMonths=18, floor=10 (matching quality-calculator). Expert/capability use halfLifeMonths=12 so they pass that option. Returns default of 50 if releaseDate is null (matching capability-calculator pattern).

Do NOT move coverage penalty logic here — that stays in `@/lib/constants/scoring` (per Phase 1 decision: constants = config values, helpers = runtime logic).
  </action>
  <verify>
    <automated>cd "F:/BotProject/AI Market Cap" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>scoring-helpers.ts exists with all 6 exports (5 functions + 1 constant). TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire expert, market-cap, capability calculators to shared helpers and unify usage calculator</name>
  <files>src/lib/scoring/expert-calculator.ts, src/lib/scoring/market-cap-calculator.ts, src/lib/scoring/capability-calculator.ts, src/lib/scoring/usage-calculator.ts</files>
  <action>
**expert-calculator.ts:**
- Delete local `BENCHMARK_IMPORTANCE` (lines 33-39), `weightedBenchmarkAvg()` (lines 41-51), `logNorm()` (lines 53-56)
- Add `import { weightedBenchmarkAvg, logNormalizeSignal, normalizeElo, computeRecencyScore } from "@/lib/scoring/scoring-helpers";`
- Replace `weightedBenchmarkAvg(...)` calls — no change needed, same function name
- Replace `logNorm(...)` calls with `logNormalizeSignal(...)` (3 call sites: likesNorm, starsNorm, newsNorm around lines 93-95)
- Replace inline ELO normalization (line 90) with `normalizeElo(inputs.eloScore!)`
- Replace inline recency calculation (lines 111-115) with `computeRecencyScore(inputs.releaseDate, { halfLifeMonths: 12 })` — expert uses 12-month half-life, not 18

**market-cap-calculator.ts:**
- Add `import { logNormalizeSignal } from "@/lib/scoring/scoring-helpers";`
- Replace all 6 inline log-normalization blocks in `computePopularityScore()` (lines 90-123) with `logNormalizeSignal(value, max)` calls. The trending score normalization (line 121) is linear not log — leave that one as-is.

**capability-calculator.ts:**
- Add `import { normalizeElo, computeRecencyScore } from "@/lib/scoring/scoring-helpers";`
- Replace inline ELO normalization (line 113) with `normalizeElo(inputs.eloScore!)`
- Replace inline recency calculation (lines 119-122) with `computeRecencyScore(inputs.releaseDate, { halfLifeMonths: 12 })` — capability uses 12-month half-life

**usage-calculator.ts:**
- Add `import { logNormalizeSignal } from "@/lib/scoring/scoring-helpers";`
- Delete local `logNorm()` function (lines 47-50)
- Merge the duplicated open/proprietary branches (lines 93-138) into a single code path:
  - Process all 6 signal types sequentially (downloads, likes, stars, news, usage/MAU, trending)
  - Each signal conditionally selects the normalization max from the appropriate pool: e.g., downloads uses `stats.openMaxDownloads` for open models, `stats.openMaxDownloads` for proprietary too (both currently use the open pool for downloads)
  - The key difference is which max to use per signal based on `isOpenWeights`, NOT duplicating the entire block
  - Preserve identical scoring output — same weights, same normalization pools, same reweighting logic
  - Use `logNormalizeSignal()` from shared helpers for all log-normalized signals

**Zero behavior change constraint:** All scoring output must be identical. The usage calculator refactor must preserve dual-pool normalization — only the code structure changes, not the math.
  </action>
  <verify>
    <automated>cd "F:/BotProject/AI Market Cap" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 4 calculators import from scoring-helpers.ts. No local logNorm/weightedBenchmarkAvg/BENCHMARK_IMPORTANCE duplicates remain. Usage calculator has single code path with conditional pool selection. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes clean (zero errors)
2. `grep -r "function logNorm" src/lib/scoring/` returns only scoring-helpers.ts
3. `grep -r "BENCHMARK_IMPORTANCE" src/lib/scoring/` returns only scoring-helpers.ts definition + quality-calculator import (quality wired in Plan 02)
4. Usage calculator has no duplicated open/proprietary blocks — single sequential signal processing
</verification>

<success_criteria>
- scoring-helpers.ts exports: logNormalizeSignal, addSignal, weightedBenchmarkAvg, normalizeElo, computeRecencyScore, BENCHMARK_IMPORTANCE
- expert-calculator has zero local helper duplicates (no logNorm, no weightedBenchmarkAvg, no BENCHMARK_IMPORTANCE)
- market-cap-calculator uses logNormalizeSignal for all log-normalized signals
- capability-calculator uses normalizeElo and computeRecencyScore from helpers
- usage-calculator has ONE code path processing all signal types
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-scoring-simplification/02-01-SUMMARY.md`
</output>
