---
phase: 02-scoring-simplification
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/scoring/quality-calculator.ts
  - src/lib/scoring/balanced-calculator.ts
  - src/lib/scoring/agent-score-calculator.ts
autonomous: true
requirements: [SCORE-04, SCORE-05]

must_haves:
  truths:
    - "calculateQualityScore() is a coordinator that calls sub-functions — no inline signal computation"
    - "Each sub-function (computePopularitySignal, computeBenchmarkSignal, computeEloSignal, computeRecencySignal, computeCommunitySignal, computeOpennessSignal) is under 50 lines"
    - "No sub-function exceeds 4 nesting levels"
    - "computeCommunitySignal is an exported standalone function usable outside quality-calculator"
    - "quality-calculator imports BENCHMARK_IMPORTANCE and weightedBenchmarkAvg from scoring-helpers (no local duplicates)"
    - "npx tsc --noEmit passes clean"
  artifacts:
    - path: "src/lib/scoring/quality-calculator.ts"
      provides: "Decomposed quality score calculator with sub-functions"
      exports: ["calculateQualityScore", "computeNormalizationStats", "computeRankings", "computeCommunitySignal"]
    - path: "src/lib/scoring/quality-calculator.ts"
      provides: "Each sub-function under 50 lines"
      contains: "function computePopularitySignal"
  key_links:
    - from: "src/lib/scoring/quality-calculator.ts"
      to: "src/lib/scoring/scoring-helpers.ts"
      via: "import shared helpers"
      pattern: "from.*scoring-helpers"
    - from: "src/lib/scoring/quality-calculator.ts"
      to: "src/lib/constants/scoring.ts"
      via: "import coverage penalty"
      pattern: "from.*constants/scoring"
---

<objective>
Decompose calculateQualityScore() into readable sub-functions and extract computeCommunitySignal() as a standalone exported function. Wire quality-calculator to use shared helpers from Plan 01.

Purpose: Break the 140-line monolithic quality scoring function into focused sub-functions under 50 lines each (SCORE-04), and make computeCommunitySignal() independently importable (SCORE-05).
Output: Refactored quality-calculator.ts with 6 sub-functions + coordinator; all calculators using shared helpers.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scoring-simplification/02-CONTEXT.md
@.planning/phases/02-scoring-simplification/02-01-SUMMARY.md

@src/lib/scoring/quality-calculator.ts
@src/lib/scoring/scoring-helpers.ts

<interfaces>
<!-- From scoring-helpers.ts created in Plan 01 -->
From src/lib/scoring/scoring-helpers.ts:
```typescript
export const BENCHMARK_IMPORTANCE: Record<string, number>;
export function logNormalizeSignal(value: number, max: number): number;
export function addSignal(signals: Array<{name: string; score: number; weight: number}>, name: string, score: number, weight: number): void;
export function weightedBenchmarkAvg(scores: Array<{slug: string; score: number}>): number;
export function normalizeElo(eloScore: number): number;
export function computeRecencyScore(releaseDate: string | null, options?: { halfLifeMonths?: number; floor?: number }): number;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose calculateQualityScore into sub-functions and extract computeCommunitySignal</name>
  <files>src/lib/scoring/quality-calculator.ts</files>
  <action>
Refactor quality-calculator.ts to decompose `calculateQualityScore()` into a coordinator calling sub-functions.

**Step 1 — Add imports from scoring-helpers:**
```typescript
import { logNormalizeSignal, addSignal, weightedBenchmarkAvg, normalizeElo, computeRecencyScore } from "@/lib/scoring/scoring-helpers";
```

**Step 2 — Delete local duplicates:**
- Delete local `BENCHMARK_IMPORTANCE` constant (lines 106-140)
- Delete local `computeWeightedBenchmarkAvg()` function (lines 142-154) — replaced by shared `weightedBenchmarkAvg()`

**Step 3 — Create sub-functions** (each must be under 50 lines, max 4 nesting levels):

Define a shared signal return type at the top of the file:
```typescript
type Signal = { name: string; score: number; weight: number } | null;
```

1. `computePopularitySignal(inputs: QualityInputs, stats: NormalizationStats, weights: WeightProfile, isHfAvailable: boolean): Signal`
   - Extracts lines 208-217 (HF downloads log-normalization)
   - Uses `logNormalizeSignal(inputs.hfDownloads!, stats.maxDownloads)` from helpers
   - Returns null if not available or no downloads

2. `computeBenchmarkSignal(inputs: QualityInputs, weights: WeightProfile): Signal`
   - Extracts lines 220-227 (benchmark score computation)
   - Uses `weightedBenchmarkAvg()` from helpers for individual scores
   - Falls back to `inputs.avgBenchmarkScore`
   - Returns null if no benchmark data

3. `computeEloSignal(inputs: QualityInputs, weights: WeightProfile, hasBenchmarks: boolean): Signal`
   - Extracts lines 231-242 (ELO normalization + benchmark weight absorption)
   - Uses `normalizeElo()` from helpers
   - If no benchmarks, absorbs benchmark weight into ELO weight
   - Returns null if no ELO score

4. `computeRecencySignal(inputs: QualityInputs, weights: WeightProfile): Signal`
   - Extracts lines 245-251 (recency decay)
   - Uses `computeRecencyScore(inputs.releaseDate, { halfLifeMonths: 18, floor: 10 })` from helpers — quality uses 18-month half-life
   - Returns null if no release date

5. `computeCommunitySignal(inputs: QualityInputs, stats: NormalizationStats, weights: WeightProfile, isProprietary: boolean, isHfAvailable: boolean): Signal` — **EXPORTED**
   - Extracts lines 253-286 (likes + news + trending)
   - Uses `logNormalizeSignal()` from helpers for likes and news normalization
   - Handles proprietary vs open model distinction
   - Returns null if no community data available
   - Per SCORE-05: this is an exported standalone function. Place it above `calculateQualityScore` and add `export` keyword.

6. `computeOpennessSignal(inputs: QualityInputs, weights: WeightProfile): Signal`
   - Extracts lines 289-294 (open/proprietary bonus)
   - Always returns a signal (never null): open=100, proprietary=50

**Step 4 — Rewrite calculateQualityScore as coordinator:**
The coordinator function:
1. Resolves weights and flags (isProprietary, isHfAvailable)
2. Calls each sub-function
3. Filters nulls, collects into signals array using `addSignal()` helper
4. Tracks `maxWeight` for each structurally-available signal (same logic as current)
5. Computes weighted average (lines 297-303 — unchanged)
6. Applies coverage penalty (lines 306-312 — unchanged, still from `@/lib/constants/scoring`)
7. Applies proxy quality gate (lines 316-326 — keep `computeProxyQualitySignal` as local private function, it's quality-specific)
8. Blends with existing AA score (lines 331-334 — unchanged)

The coordinator should be under 60 lines. The sub-functions handle the signal logic, the coordinator handles assembly + penalty + gate.

**Zero behavior change constraint:** Every sub-function must produce exactly the same score for the same inputs. The maxWeight tracking must be preserved identically — popularity and community maxWeight are conditional on isHfAvailable/isProprietary.
  </action>
  <verify>
    <automated>cd "F:/BotProject/AI Market Cap" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>calculateQualityScore() is a coordinator calling 6 sub-functions. Each sub-function is under 50 lines. computeCommunitySignal is exported. No local BENCHMARK_IMPORTANCE or computeWeightedBenchmarkAvg duplicates. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Final verification — no remaining duplication across all 7 calculators</name>
  <files>src/lib/scoring/balanced-calculator.ts, src/lib/scoring/agent-score-calculator.ts</files>
  <action>
Verify balanced-calculator.ts and agent-score-calculator.ts for any remaining duplication opportunities:

**balanced-calculator.ts:** This calculator is a meta-ranker that combines ranks from other lenses. It has NO scoring helpers to extract (no logNorm, no benchmarks, no ELO). Verify it has no duplication with scoring-helpers — it should be unchanged. If it imports anything from other calculators that now lives in scoring-helpers, update the import.

**agent-score-calculator.ts:** Per Phase 1 decision, this was intentionally NOT modified because its "continuous sqrt formula is algorithmic not a lookup table." Verify:
- Its coverage penalty (line 277: `0.5 + 0.5 * Math.sqrt(coverageFraction)`) is a continuous formula, NOT a lookup table — this is correct and should NOT be changed to use getCoveragePenalty
- Its `computePercentile()` helper is agent-specific, not shared
- No logNorm, weightedBenchmarkAvg, or ELO normalization patterns exist here

Run final comprehensive checks:
1. `npx tsc --noEmit` — must pass clean
2. Verify no duplicate logNorm functions exist outside scoring-helpers.ts
3. Verify no duplicate BENCHMARK_IMPORTANCE exists outside scoring-helpers.ts
4. Verify no inline ELO normalization `(eloScore - 800) / (1400 - 800)` exists outside scoring-helpers.ts
5. Verify no inline recency decay `Math.exp(-ageMonths / N)` exists outside scoring-helpers.ts
6. Verify computeCommunitySignal is exported from quality-calculator.ts
  </action>
  <verify>
    <automated>cd "F:/BotProject/AI Market Cap" && npx tsc --noEmit && echo "TSC_CLEAN" && grep -rn "function logNorm\b" src/lib/scoring/ | grep -v scoring-helpers && echo "NO_DUPES_LOGNORM" || echo "CLEAN_LOGNORM" && grep -rn "BENCHMARK_IMPORTANCE" src/lib/scoring/ | grep -v scoring-helpers | grep -v "from.*scoring-helpers" && echo "DUPES_FOUND" || echo "CLEAN_BENCHMARK"</automated>
  </verify>
  <done>All 7 calculators verified. No remaining duplication of logNorm, weightedBenchmarkAvg, BENCHMARK_IMPORTANCE, ELO normalization, or recency calculation outside scoring-helpers.ts. computeCommunitySignal is exported. TypeScript compiles clean. Zero behavior changes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes clean
2. `calculateQualityScore()` body is under 60 lines (coordinator only)
3. Each sub-function is under 50 lines: computePopularitySignal, computeBenchmarkSignal, computeEloSignal, computeRecencySignal, computeCommunitySignal, computeOpennessSignal
4. `grep "export.*computeCommunitySignal" src/lib/scoring/quality-calculator.ts` confirms export
5. No inline `(eloScore - 800)` pattern outside scoring-helpers.ts
6. No inline `Math.exp(-ageMonths` pattern outside scoring-helpers.ts
7. No local BENCHMARK_IMPORTANCE or weightedBenchmarkAvg in any calculator
</verification>

<success_criteria>
- calculateQualityScore is a coordinator calling 6 named sub-functions
- Every sub-function is under 50 lines with max 4 nesting levels
- computeCommunitySignal is exported and standalone-callable
- quality-calculator imports from scoring-helpers (no local duplicates)
- All 7 calculators verified — no remaining helper duplication
- TypeScript compiles clean across entire codebase
</success_criteria>

<output>
After completion, create `.planning/phases/02-scoring-simplification/02-02-SUMMARY.md`
</output>
