---
phase: 13-component-decomposition-react-memo
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/models/ranking-weight-controls.tsx
  - src/components/models/ranking-weight-helpers.ts
  - src/components/models/leaderboard-explorer.tsx
  - src/components/models/leaderboard-controls.tsx
  - src/components/models/leaderboard-table.tsx
autonomous: true
requirements: [DECOMP-04, PERF-02]

must_haves:
  truths:
    - "Ranking weight controls renders weight sliders with redistribute/reset behavior"
    - "Leaderboard explorer renders lens tabs, category filter, search, and sortable paginated table"
    - "ScoreBar in leaderboard table is wrapped with React.memo and skips re-renders on stable props"
    - "No component file exceeds 300 lines"
    - "Existing ranking-weight-controls.test.tsx passes without import changes"
    - "All existing tests pass after decomposition"
  artifacts:
    - path: "src/components/models/ranking-weight-controls.tsx"
      provides: "Weight controls component with WeightRow sub-component"
      max_lines: 300
    - path: "src/components/models/ranking-weight-helpers.ts"
      provides: "Pure helper functions and type definitions for weight calculations"
      min_lines: 80
    - path: "src/components/models/leaderboard-explorer.tsx"
      provides: "Parent shell with table setup and column definitions"
      max_lines: 300
    - path: "src/components/models/leaderboard-controls.tsx"
      provides: "Lens toggle tabs, category filter, search input"
      min_lines: 40
    - path: "src/components/models/leaderboard-table.tsx"
      provides: "Table rendering with pagination, contains memo'd ScoreBar"
      min_lines: 40
  key_links:
    - from: "src/components/models/ranking-weight-controls.tsx"
      to: "ranking-weight-helpers.ts"
      via: "import pure functions and types"
      pattern: "import.*from.*ranking-weight-helpers"
    - from: "src/components/models/leaderboard-explorer.tsx"
      to: "leaderboard-controls.tsx"
      via: "import and render controls above table"
      pattern: "import.*LeaderboardControls.*from.*leaderboard-controls"
    - from: "src/components/models/leaderboard-explorer.tsx"
      to: "leaderboard-table.tsx"
      via: "import and render table with pagination"
      pattern: "import.*LeaderboardTable.*from.*leaderboard-table"
---

<objective>
Decompose ranking-weight-controls (517 lines) and leaderboard-explorer (457 lines) below 300 lines each, and apply React.memo to ScoreBar for re-render optimization.

Purpose: These are the final two DECOMP-04 targets, both in src/components/models/. ranking-weight-controls has ~130 lines of pure helper functions and type definitions that can be extracted to a separate non-React file. leaderboard-explorer has natural split points: controls area (lens tabs, category filter, search) and table area (data table with pagination). ScoreBar renders per-cell per-row (50+ instances per page) and is the second-highest React.memo candidate after ComparisonRow.

Output: ranking-weight-controls.tsx reduced to ~280 lines, leaderboard-explorer.tsx reduced to ~200 lines, 3 new files, ScoreBar memo'd.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-component-decomposition-react-memo/13-RESEARCH.md

Key context:
- ranking-weight-controls.tsx has an existing test: src/components/models/ranking-weight-controls.test.tsx (159 lines). Test imports `import RankingWeightControls from './ranking-weight-controls'` (default import). This import MUST continue to work.
- ranking-weight-controls.tsx already has WeightRow as an internal sub-component (~90 lines). WeightRow stays in the same file. The extraction target is pure helper functions + types.
- Helper functions to extract: getRawValue (~15 lines), computePercentiles (~30 lines), redistributeWeights (~35 lines). These are pure functions with no React dependency.
- Types to extract: RankableModel interface, WeightKey type, WeightSignal interface, DEFAULT_WEIGHTS, WEIGHT_KEYS, MIN_WEIGHT, MAX_WEIGHT, STEP constants.
- leaderboard-explorer.tsx uses @tanstack/react-table. Column definitions (useMemo ~140 lines) stay in the parent since they reference the table instance.
- ScoreBar is a tiny function (~20 lines) defined inside leaderboard-explorer.tsx. It renders a colored progress bar per cell. Move to leaderboard-table.tsx and wrap with React.memo.
- LeaderboardModel interface and LENS_TABS, CATEGORY_TABS constants defined in leaderboard-explorer.tsx.
- getLensRank and getLensScore helper functions used in column definitions.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ranking-weight-controls helpers to separate file</name>
  <files>
    src/components/models/ranking-weight-controls.tsx,
    src/components/models/ranking-weight-helpers.ts
  </files>
  <action>
    Create **ranking-weight-helpers.ts** (~130 lines, NOT a React component -- plain .ts file):

    Move from ranking-weight-controls.tsx:
    - `RankableModel` interface (lines 17-40)
    - `WeightKey` type (derived from DEFAULT_WEIGHTS keys or explicitly defined)
    - `WeightSignal` interface (lines 53-58)
    - `DEFAULT_WEIGHTS` constant (lines 59-86)
    - `WEIGHT_KEYS` array (lines 87-93)
    - `MIN_WEIGHT`, `MAX_WEIGHT`, `STEP` constants (lines 95-97)
    - `getRawValue` function (lines 103-116)
    - `computePercentiles` function (lines 127-158)
    - `redistributeWeights` function (if it exists as a standalone function -- check the file; it might be inline in a handler)

    Export all of these as named exports.

    Rewrite **ranking-weight-controls.tsx**:
    - Add `import { RankableModel, WeightKey, WeightSignal, DEFAULT_WEIGHTS, WEIGHT_KEYS, MIN_WEIGHT, MAX_WEIGHT, STEP, getRawValue, computePercentiles } from "./ranking-weight-helpers";`
    - Remove the moved definitions
    - Keep: "use client" directive, RankingWeightControlsProps interface, the main RankingWeightControls component, WeightRow sub-component, all useState/useEffect/useMemo hooks, all event handlers
    - Keep the `export default function RankingWeightControls(...)` -- do NOT change to named export (test uses default import)

    Target: ranking-weight-controls.tsx reduced from 517 to ~280 lines.

    CRITICAL: The test file imports `import RankingWeightControls from './ranking-weight-controls'`. Do NOT change this export. Keep it as `export default function RankingWeightControls(...)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>ranking-weight-controls.tsx under 300 lines. ranking-weight-helpers.ts contains all pure functions and types. Existing test passes unchanged. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Decompose leaderboard-explorer and apply React.memo to ScoreBar</name>
  <files>
    src/components/models/leaderboard-explorer.tsx,
    src/components/models/leaderboard-controls.tsx,
    src/components/models/leaderboard-table.tsx
  </files>
  <action>
    1. **leaderboard-controls.tsx** (~80 lines): Extract the controls area above the table. "use client" component. Props:
    ```typescript
    interface LeaderboardControlsProps {
      activeLens: RankingLens;
      setActiveLens: (lens: RankingLens) => void;
      categoryFilter: string;
      setCategoryFilter: (cat: string) => void;
      searchQuery: string;
      setSearchQuery: (q: string) => void;
    }
    ```
    Contains: lens tab buttons (LENS_TABS), category filter tabs (CATEGORY_TABS), search input. Move LENS_TABS and CATEGORY_TABS constants here. Export the RankingLens type from here (or keep it in leaderboard-explorer.tsx and export it).

    Import lucide icons needed for lens tabs. Import Badge, Input from UI components.

    2. **leaderboard-table.tsx** (~120 lines): Extract the table rendering and pagination area. "use client" component. Props:
    ```typescript
    interface LeaderboardTableProps {
      table: ReturnType<typeof useReactTable<LeaderboardModel>>;
    }
    ```
    OR receive the table instance type from @tanstack/react-table: `Table<LeaderboardModel>`.

    Move ScoreBar here and wrap with React.memo:
    ```typescript
    const ScoreBar = memo(function ScoreBar({ value, max = 100 }: { value: number | null; max?: number }) {
      if (value === null) return <span className="text-muted-foreground">—</span>;
      const pct = Math.min((value / max) * 100, 100);
      const color = pct >= 80 ? "bg-gain" : pct >= 60 ? "bg-chart-2" : pct >= 40 ? "bg-chart-4" : "bg-loss";
      return (
        <div className="flex items-center gap-2">
          <div className="h-2 w-16 rounded bg-muted overflow-hidden">
            <div className={`h-full rounded ${color}`} style={{ width: `${pct}%` }} />
          </div>
          <span className="text-xs tabular-nums">{value.toFixed(1)}</span>
        </div>
      );
    });
    ```
    ScoreBar is used in column cell renderers. Since column definitions reference ScoreBar and are defined via useMemo in the parent, and the columns pass ScoreBar in their `cell` property, ScoreBar must be accessible to the column definitions. Options:
    - Option A: Keep ScoreBar in leaderboard-explorer.tsx. But then it doesn't move with the table.
    - Option B (preferred): Export ScoreBar from leaderboard-table.tsx, import it in leaderboard-explorer.tsx for use in column definitions. The column definitions stay in the parent because they access `activeLens` state.

    Go with Option B: export ScoreBar from leaderboard-table.tsx, import it in leaderboard-explorer.tsx.

    Contains: the table element rendering (thead with headerGroups, tbody with rows, flexRender calls), pagination controls (page buttons, page indicator).

    3. Rewrite **leaderboard-explorer.tsx** (~200 lines): Keep "use client", all state (activeLens, sorting, categoryFilter, searchQuery, customSortedModels), filteredModels useMemo, column definitions useMemo (references ScoreBar imported from leaderboard-table), useReactTable call, useEffect for custom sorting. Keep LeaderboardModel interface and export it. Keep LeaderboardExplorerProps. Keep getLensRank, getLensScore helpers (used in column defs). Import LeaderboardControls and LeaderboardTable, render them. Import ScoreBar from leaderboard-table for column definitions.

    Keep `export default function LeaderboardExplorer(...)` as default export.

    Target: leaderboard-explorer.tsx under 250 lines.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>leaderboard-explorer.tsx under 300 lines. leaderboard-controls.tsx and leaderboard-table.tsx exist. ScoreBar wrapped with React.memo. TypeScript compiles clean. All tests pass.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` passes with zero errors
2. `npx vitest run` passes all existing tests -- specifically ranking-weight-controls.test.tsx must pass unchanged
3. `wc -l src/components/models/ranking-weight-controls.tsx src/components/models/leaderboard-explorer.tsx` both show under 300 lines
4. `grep -r "memo(function ScoreBar" src/components/models/leaderboard-table.tsx` confirms React.memo wrapping
5. All new files exist: ranking-weight-helpers.ts, leaderboard-controls.tsx, leaderboard-table.tsx
</verification>

<success_criteria>
- ranking-weight-controls.tsx decomposed from 517 to under 300 lines via helper extraction
- leaderboard-explorer.tsx decomposed from 457 to under 250 lines with controls and table sub-components
- ScoreBar wrapped with React.memo in leaderboard-table.tsx
- ranking-weight-controls.test.tsx passes without any modifications
- Default exports preserved for both main components
- TypeScript compiles clean, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-component-decomposition-react-memo/13-04-SUMMARY.md`
</output>
