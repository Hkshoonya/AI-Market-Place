---
phase: 13-component-decomposition-react-memo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(catalog)/models/[slug]/page.tsx
  - src/app/(catalog)/models/[slug]/_components/model-header.tsx
  - src/app/(catalog)/models/[slug]/_components/model-stats-row.tsx
  - src/app/(catalog)/models/[slug]/_components/benchmarks-tab.tsx
  - src/app/(catalog)/models/[slug]/_components/pricing-tab.tsx
  - src/app/(catalog)/models/[slug]/_components/trading-tab.tsx
  - src/app/(catalog)/models/[slug]/_components/trends-tab.tsx
  - src/app/(catalog)/models/[slug]/_components/news-tab.tsx
  - src/app/(catalog)/models/[slug]/_components/details-tab.tsx
  - src/app/(catalog)/models/[slug]/_components/changelog-tab.tsx
  - src/app/(auth)/settings/settings-form.tsx
  - src/app/(auth)/settings/_components/account-info-card.tsx
  - src/app/(auth)/settings/_components/email-change-card.tsx
  - src/app/(auth)/settings/_components/password-change-card.tsx
  - src/app/(auth)/settings/_components/notification-prefs-card.tsx
  - src/app/(auth)/settings/_components/danger-zone-card.tsx
autonomous: true
requirements: [DECOMP-01, DECOMP-03]

must_haves:
  truths:
    - "Model detail page renders all 8 tab sections correctly with data passed as props"
    - "Settings form renders all 5 card sections with independent state per card"
    - "No component file exceeds 300 lines"
    - "All existing tests pass after decomposition"
  artifacts:
    - path: "src/app/(catalog)/models/[slug]/page.tsx"
      provides: "Parent shell with data fetching, metadata, JSON-LD, layout, and tab container"
      max_lines: 300
    - path: "src/app/(catalog)/models/[slug]/_components/benchmarks-tab.tsx"
      provides: "Benchmark scores table + ELO ratings section"
      min_lines: 50
    - path: "src/app/(auth)/settings/settings-form.tsx"
      provides: "Auth guard + layout shell importing 5 card sub-components"
      max_lines: 150
    - path: "src/app/(auth)/settings/_components/password-change-card.tsx"
      provides: "Self-contained password change form with own state"
      min_lines: 40
  key_links:
    - from: "src/app/(catalog)/models/[slug]/page.tsx"
      to: "_components/*.tsx"
      via: "import and render as children of TabsContent"
      pattern: "import.*from.*_components"
    - from: "src/app/(auth)/settings/settings-form.tsx"
      to: "_components/*.tsx"
      via: "import and render card sub-components"
      pattern: "import.*from.*_components"
---

<objective>
Decompose the model detail page (878 lines) and settings form (681 lines) into focused sub-components under 300 lines each.

Purpose: These are the two largest page-level components. The model detail page is a server component where each tab's JSX is a natural extraction point. The settings form is a client component where each Card section has independent state and handlers, making each card a self-contained sub-component.

Output: Model detail page reduced to ~250 lines (data fetching + layout shell), 9 tab/section sub-components in `_components/` directory. Settings form reduced to ~120 lines, 5 card sub-components in `_components/` directory.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-component-decomposition-react-memo/13-RESEARCH.md

Key context:
- Model detail page (page.tsx) is a SERVER component -- extracted tab sub-components MUST NOT have "use client" directive
- Tab sub-components receive pre-fetched data as props (no data fetching in sub-components)
- Settings form is a CLIENT component -- each card section owns its own useState/handlers
- Use `_components/` directory prefix (Next.js private folder convention, not treated as route segments)
- Follow Phase 5 decomposition pattern: extract JSX into sibling files, define props interfaces inline
- The NotifPrefs interface and the supabase client creation stay in the parent settings-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose model detail page into tab sub-components</name>
  <files>
    src/app/(catalog)/models/[slug]/page.tsx,
    src/app/(catalog)/models/[slug]/_components/model-header.tsx,
    src/app/(catalog)/models/[slug]/_components/model-stats-row.tsx,
    src/app/(catalog)/models/[slug]/_components/benchmarks-tab.tsx,
    src/app/(catalog)/models/[slug]/_components/pricing-tab.tsx,
    src/app/(catalog)/models/[slug]/_components/trading-tab.tsx,
    src/app/(catalog)/models/[slug]/_components/trends-tab.tsx,
    src/app/(catalog)/models/[slug]/_components/news-tab.tsx,
    src/app/(catalog)/models/[slug]/_components/details-tab.tsx,
    src/app/(catalog)/models/[slug]/_components/changelog-tab.tsx
  </files>
  <action>
    Create `_components/` directory under `src/app/(catalog)/models/[slug]/`.

    Extract 9 sub-components from page.tsx. Each is a SERVER component (NO "use client" directive). Each receives pre-fetched data as typed props.

    1. **model-header.tsx** (~80 lines): The header section from the page (model name, badges, provider logo, description, action buttons including website link, GitHub link, ShareModel, ModelActions). Props: model object fields needed for header display (name, provider, description, website_url, github_url, is_open_weights, category, slug, id, short_description), plus catConfig.

    2. **model-stats-row.tsx** (~50 lines): The stats grid with 7 stat cards (lines ~326-355 in original). Props: stats array of {label, value, icon} objects. Keep the stats array construction in page.tsx and pass it as a prop.

    3. **benchmarks-tab.tsx** (~140 lines): TabsContent "benchmarks" (lines 374-513). Contains benchmark score bars + ELO ratings card. Props: benchmarkScores array, eloRatings array, bestElo object. Import BenchmarkRadar from @/components/charts/benchmark-radar.

    4. **pricing-tab.tsx** (~60 lines): TabsContent "pricing" (lines 516-578). Props: pricingData array. Import PriceComparison chart. Import formatTokenPrice.

    5. **trading-tab.tsx** (~40 lines): TabsContent "trading" (lines 590-627). Props: model slug and name. Import TradingChart (already a client component) and ModelOverview (already a client component).

    6. **trends-tab.tsx** (~40 lines): TabsContent "trends" (lines 630-667). Props: snapshots array, model slug and name. Import QualityTrend and DownloadsTrend charts.

    7. **news-tab.tsx** (~90 lines): TabsContent "news" (lines 670-755). Props: modelNews array, model provider. Import NewsCard. Group news by date logic stays in this component.

    8. **details-tab.tsx** (~60 lines): TabsContent "details" (lines 758-820). Props: model object fields (modalities, capabilities, parameters, context_window, license, release_date, training data fields).

    9. **changelog-tab.tsx** (~35 lines): TabsContent "changelog" (lines 823-857). Props: updates array. Simple timeline rendering.

    Rewrite page.tsx to:
    - Keep generateMetadata function (lines 48-90)
    - Keep ModelDetailPage function with all data fetching (lines 92-230)
    - Keep JSON-LD script tag
    - Keep the layout shell (breadcrumbs, header area, tabs container, TabsList, similar models, comments section)
    - Import and render each extracted sub-component inside its TabsContent wrapper
    - The TabsContent wrappers stay in page.tsx -- sub-components render the CONTENT inside each tab

    IMPORTANT: All extracted sub-components are server components. Do NOT add "use client". Do NOT import useState/useEffect. They receive data as props only. Components like TradingChart, ModelOverview, CommentsSection that are already client components continue to be imported from their existing locations.

    Define proper TypeScript interfaces for each sub-component's props. Move any inline type casts (like `as { ... }`) to the data preparation section in page.tsx so sub-components receive cleanly typed data.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>page.tsx is under 300 lines. 9 sub-component files exist in _components/ directory. TypeScript compiles clean. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Decompose settings form into card sub-components</name>
  <files>
    src/app/(auth)/settings/settings-form.tsx,
    src/app/(auth)/settings/_components/account-info-card.tsx,
    src/app/(auth)/settings/_components/email-change-card.tsx,
    src/app/(auth)/settings/_components/password-change-card.tsx,
    src/app/(auth)/settings/_components/notification-prefs-card.tsx,
    src/app/(auth)/settings/_components/danger-zone-card.tsx
  </files>
  <action>
    Create `_components/` directory under `src/app/(auth)/settings/`.

    Extract 5 card sub-components. Each is a CLIENT component ("use client") that owns its own local state.

    1. **account-info-card.tsx** (~35 lines): Read-only account info card (lines 309-341). Props: user email, profile display_name, profile avatar_url, profile created_at. No state -- pure display.

    2. **email-change-card.tsx** (~50 lines): Email change form card (lines 343-385). Props: none needed (uses its own supabase client). Owns: newEmail, emailLoading, emailMessage, emailError state. Has handleEmailChange handler. Creates its own `createClient()` supabase instance.

    3. **password-change-card.tsx** (~80 lines): Password change form card (lines 387-456). Props: userEmail (needed for signInWithPassword verification step). Owns: currentPassword, newPassword, confirmPassword, passwordLoading, passwordMessage, passwordError state. Has handlePasswordChange handler. Creates its own `createClient()` supabase instance.

    4. **notification-prefs-card.tsx** (~110 lines): Notification preferences card (lines 458-586). Props: none. Owns: notifPrefs, notifLoading, notifSaving, notifMessage state. Has fetchNotifPrefs (called in useEffect on mount) and handleSaveNotifPrefs. Move the NotifPrefs interface INTO this file since it's only used here.

    5. **danger-zone-card.tsx** (~70 lines): Sign out + delete account card (lines ~588-681). Props: signOut function (from useAuth). Owns: showDeleteConfirm, deleteConfirmText, deleteLoading, deleteError state. Has handleSignOut and handleDeleteAccount handlers. Uses router for redirect after account deletion.

    Rewrite settings-form.tsx to:
    - Keep "use client" directive
    - Keep auth guard logic (loading state, redirect if no user)
    - Keep breadcrumb/header layout
    - Import and render 5 card sub-components in the space-y-6 container
    - Pass minimal props: user email to password-change-card, profile data to account-info-card, signOut to danger-zone-card
    - Remove all state declarations that moved to sub-components
    - Remove all handler functions that moved to sub-components
    - Remove the NotifPrefs interface (moved to notification-prefs-card)

    Target: settings-form.tsx reduced to ~120 lines.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>settings-form.tsx is under 150 lines. 5 card sub-component files exist in _components/ directory. Each card owns its state independently. TypeScript compiles clean. All tests pass.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` passes with zero errors
2. `npx vitest run` passes all existing tests (170+)
3. `wc -l src/app/(catalog)/models/[slug]/page.tsx` shows under 300 lines
4. `wc -l src/app/(auth)/settings/settings-form.tsx` shows under 150 lines
5. All sub-component files exist in their respective `_components/` directories
</verification>

<success_criteria>
- Model detail page decomposed from 878 lines to under 300 lines with 9 sub-components
- Settings form decomposed from 681 lines to under 150 lines with 5 sub-components
- No "use client" in any model detail sub-component (server component boundary preserved)
- Each settings card sub-component has "use client" and manages its own state
- TypeScript compiles clean, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-component-decomposition-react-memo/13-01-SUMMARY.md`
</output>
