---
phase: 07-error-handling-logging
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/lib/cron-tracker.ts
  - src/lib/compute-scores/fetch-inputs.ts
  - src/lib/marketplace/purchase-handlers.ts
  - src/lib/marketplace/auctions/english.ts
  - src/lib/marketplace/auctions/dutch.ts
  - src/lib/marketplace/escrow.ts
  - src/lib/payments/withdraw.ts
  - src/lib/payments/chains/solana.ts
  - src/lib/payments/chains/evm.ts
  - src/lib/agents/runtime.ts
  - src/lib/agents/registry.ts
  - src/lib/data-sources/registry.ts
  - src/lib/data-sources/model-matcher.ts
  - src/lib/data-sources/orchestrator.ts
autonomous: true
requirements:
  - ERR-01
  - LOG-01
  - LOG-03

must_haves:
  truths:
    - "No console.error or console.warn calls remain in src/lib/ (except logging.ts internal mirror)"
    - "Every former silent .catch(() => {}) in orchestrator now logs with systemLog"
    - "All cron-tracker, adapter, and agent log calls include a source identifier"
  artifacts:
    - path: "src/lib/cron-tracker.ts"
      provides: "Tagged logger replacing 4 console.error calls"
      contains: "createTaggedLogger"
    - path: "src/lib/marketplace/auctions/english.ts"
      provides: "Tagged logger replacing 5 console.error calls"
      contains: "createTaggedLogger"
    - path: "src/lib/data-sources/orchestrator.ts"
      provides: "Silent catches replaced with systemLog calls"
      contains: "systemLog"
  key_links:
    - from: "src/lib/cron-tracker.ts"
      to: "src/lib/logging.ts"
      via: "import createTaggedLogger"
      pattern: "createTaggedLogger"
    - from: "src/lib/data-sources/orchestrator.ts"
      to: "src/lib/logging.ts"
      via: "import systemLog or createTaggedLogger"
      pattern: "systemLog|createTaggedLogger"
---

<objective>
Replace all console.error/warn calls in src/lib/ with structured logging and fix silent catch blocks in server-side library code.

Purpose: Satisfy LOG-01 (structured logger in src/lib/), LOG-03 (tagged loggers with source ID for adapters/cron), and ERR-01 (no silent catches on server side).
Output: All 15 library files migrated from console.error/warn to createTaggedLogger or systemLog.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-error-handling-logging/07-CONTEXT.md
@.planning/phases/07-error-handling-logging/07-01-PLAN.md

@src/lib/logging.ts
</context>

<interfaces>
<!-- From Plan 01 outputs -->
From src/lib/logging.ts (after Plan 01):
```typescript
export const systemLog = { info, warn, error };
export function createTaggedLogger(source: string): TaggedLogger;
export interface TaggedLogger {
  info: (message: string, metadata?: Record<string, unknown>) => Promise<string | null>;
  warn: (message: string, metadata?: Record<string, unknown>) => Promise<string | null>;
  error: (message: string, metadata?: Record<string, unknown>) => Promise<string | null>;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace console.error/warn with tagged loggers in marketplace, payments, and agents code</name>
  <files>
    src/lib/marketplace/purchase-handlers.ts,
    src/lib/marketplace/auctions/english.ts,
    src/lib/marketplace/auctions/dutch.ts,
    src/lib/marketplace/escrow.ts,
    src/lib/payments/withdraw.ts,
    src/lib/payments/chains/solana.ts,
    src/lib/payments/chains/evm.ts,
    src/lib/agents/runtime.ts,
    src/lib/agents/registry.ts
  </files>
  <action>
For each file, create a module-level tagged logger and replace all console.error/warn calls:

- `purchase-handlers.ts` (3 calls): `const log = createTaggedLogger("marketplace/purchase");` — replace 3 console.error calls with `log.error(...)`, passing error details as metadata
- `english.ts` (5 calls): `const log = createTaggedLogger("marketplace/auction-english");` — replace all console.error calls
- `dutch.ts` (3 calls): `const log = createTaggedLogger("marketplace/auction-dutch");` — replace all console.error calls
- `escrow.ts` (1 call): `const log = createTaggedLogger("marketplace/escrow");` — replace console.error
- `withdraw.ts` (1 call): `const log = createTaggedLogger("payments/withdraw");` — replace console.error
- `solana.ts` (2 calls): `const log = createTaggedLogger("payments/solana");` — replace console.error calls
- `evm.ts` (3 calls): `const log = createTaggedLogger("payments/evm");` — replace console.error calls
- `runtime.ts` (2 calls): `const log = createTaggedLogger("agents/runtime");` — replace console.error calls
- `registry.ts` (agents, 1 call): `const log = createTaggedLogger("agents/registry");` — replace console.warn

Pattern for each replacement:
```typescript
// Before:
console.error("[purchase] Order creation failed:", orderError);
// After:
log.error("Order creation failed", { error: orderError });
```

The systemLog calls are fire-and-forget (no await on the log call, same as original console.error).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 21 console.error/warn calls in marketplace/, payments/, agents/ files replaced with tagged logger calls. No console.error/warn remain in those files.</done>
</task>

<task type="auto">
  <name>Task 2: Replace console calls in data-sources, cron-tracker, compute-scores + fix orchestrator silent catches</name>
  <files>
    src/lib/cron-tracker.ts,
    src/lib/compute-scores/fetch-inputs.ts,
    src/lib/data-sources/registry.ts,
    src/lib/data-sources/model-matcher.ts,
    src/lib/data-sources/orchestrator.ts
  </files>
  <action>
1. **cron-tracker.ts** (4 console.error calls): Already imports systemLog. Create `const log = createTaggedLogger("cron-tracker");` and replace the 4 console.error calls with `log.error(...)`. Remove the direct systemLog import if no longer needed (cron-tracker already uses systemLog in some places — consolidate to tagged logger).

2. **fetch-inputs.ts** (1 console.warn call): `const log = createTaggedLogger("compute-scores");` — replace `console.warn(...)` with `log.warn(...)`.

3. **data-sources/registry.ts** (1 console.warn): `const log = createTaggedLogger("data-sources/registry");` — replace console.warn.

4. **data-sources/model-matcher.ts** (1 console.error): `const log = createTaggedLogger("data-sources/model-matcher");` — replace console.error.

5. **data-sources/orchestrator.ts** (2 silent catches): Fix the two silent `.catch(() => {})` on lines 171 and 173 (recordSyncFailure and recordSyncSuccess). Replace with:
```typescript
.catch((err: unknown) => {
  systemLog.warn("sync-orchestrator", "Failed to record sync status", {
    slug: source.slug,
    error: err instanceof Error ? err.message : String(err)
  });
})
```
Use systemLog directly (not tagged logger) here since the orchestrator context changes per-source. The key point: these must NOT be silent — they must produce a log entry. Since systemLog itself has a console fallback, there's no recursive failure risk.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All console.error/warn in cron-tracker, fetch-inputs, registries, and model-matcher replaced with tagged loggers. Orchestrator silent catches now log. Zero console.error/warn in src/lib/ except logging.ts internal mirror.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes clean
- `grep -r "console\.error\|console\.warn" src/lib/ --include="*.ts" | grep -v logging.ts | grep -v "//.*console"` returns no matches
- `grep -r "\.catch(() => {})" src/lib/` returns no matches
- `grep -r "createTaggedLogger" src/lib/ | wc -l` shows 10+ files using tagged loggers
</verification>

<success_criteria>
- Zero console.error/warn in src/lib/ (excluding logging.ts mirror and commented code)
- All 32 former console.error/warn calls use structured logging with source identifiers
- Orchestrator silent catches eliminated — each logs at minimum a warning
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-error-handling-logging/07-02-SUMMARY.md`
</output>
