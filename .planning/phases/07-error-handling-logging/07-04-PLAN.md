---
phase: 07-error-handling-logging
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/api/marketplace/auctions/[id]/accept/route.ts
  - src/app/api/marketplace/auctions/[id]/bid/route.ts
  - src/app/api/marketplace/auctions/[id]/route.ts
  - src/app/api/marketplace/auctions/route.ts
  - src/app/api/marketplace/listings/[slug]/manifest/route.ts
  - src/app/api/marketplace/listings/[slug]/pricing/route.ts
  - src/app/api/marketplace/listings/[slug]/report/route.ts
  - src/app/api/marketplace/listings/[slug]/reviews/route.ts
  - src/app/api/marketplace/listings/[slug]/route.ts
  - src/app/api/marketplace/listings/bot/route.ts
  - src/app/api/marketplace/listings/route.ts
  - src/app/api/marketplace/orders/[id]/messages/route.ts
  - src/app/api/marketplace/orders/[id]/route.ts
  - src/app/api/marketplace/orders/route.ts
  - src/app/api/marketplace/purchase/route.ts
  - src/app/api/marketplace/seller/stats/route.ts
  - src/app/api/marketplace/seller/verify/route.ts
  - src/app/api/marketplace/wallet/route.ts
  - src/app/api/seller/earnings/route.ts
  - src/app/api/seller/withdraw/route.ts
  - src/app/api/webhooks/chain-deposits/route.ts
autonomous: true
requirements:
  - ERR-02
  - LOG-02

must_haves:
  truths:
    - "All marketplace and webhook API routes use handleApiError for error responses"
    - "All console.error/warn in marketplace routes are replaced with structured logging"
    - "Webhook route uses tagged logger with webhook/ source prefix"
    - "No console.error/warn remains in any API route file"
  artifacts:
    - path: "src/app/api/marketplace/orders/[id]/route.ts"
      provides: "Marketplace route with handleApiError"
      contains: "handleApiError"
    - path: "src/app/api/webhooks/chain-deposits/route.ts"
      provides: "Webhook route with tagged logger"
      contains: "createTaggedLogger"
  key_links:
    - from: "src/app/api/marketplace/purchase/route.ts"
      to: "src/lib/api-error.ts"
      via: "import handleApiError"
      pattern: "handleApiError"
    - from: "src/app/api/webhooks/chain-deposits/route.ts"
      to: "src/lib/logging.ts"
      via: "import createTaggedLogger"
      pattern: "createTaggedLogger"
---

<objective>
Migrate all marketplace, seller, and webhook API routes to use handleApiError and structured logging.

Purpose: Complete ERR-02 (consistent ApiError pattern) and LOG-02 (structured logging) coverage for the remaining route files.
Output: ~21 route files using handleApiError + structured logging. Zero console.error/warn across all API routes.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-error-handling-logging/07-CONTEXT.md
@.planning/phases/07-error-handling-logging/07-01-PLAN.md

@src/lib/api-error.ts
@src/lib/logging.ts
</context>

<interfaces>
<!-- From Plan 01 outputs -->
From src/lib/api-error.ts (after Plan 01):
```typescript
export class ApiError extends Error {
  constructor(public statusCode: number, message: string);
}
export function handleApiError(error: unknown, source: string): Response;
```

From src/lib/logging.ts (after Plan 01):
```typescript
export function createTaggedLogger(source: string): TaggedLogger;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate marketplace API routes (auctions, listings, orders, purchase) to handleApiError</name>
  <files>
    src/app/api/marketplace/auctions/[id]/accept/route.ts,
    src/app/api/marketplace/auctions/[id]/bid/route.ts,
    src/app/api/marketplace/auctions/[id]/route.ts,
    src/app/api/marketplace/auctions/route.ts,
    src/app/api/marketplace/listings/[slug]/manifest/route.ts,
    src/app/api/marketplace/listings/[slug]/pricing/route.ts,
    src/app/api/marketplace/listings/[slug]/report/route.ts,
    src/app/api/marketplace/listings/[slug]/reviews/route.ts,
    src/app/api/marketplace/listings/[slug]/route.ts,
    src/app/api/marketplace/listings/bot/route.ts,
    src/app/api/marketplace/listings/route.ts,
    src/app/api/marketplace/orders/[id]/messages/route.ts,
    src/app/api/marketplace/orders/[id]/route.ts,
    src/app/api/marketplace/orders/route.ts,
    src/app/api/marketplace/purchase/route.ts,
    src/app/api/marketplace/seller/stats/route.ts,
    src/app/api/marketplace/seller/verify/route.ts,
    src/app/api/marketplace/wallet/route.ts
  </files>
  <action>
Same mechanical migration pattern for all marketplace routes:

1. **Add imports**: `import { handleApiError } from "@/lib/api-error";`
   For routes with multiple log calls, add: `import { createTaggedLogger } from "@/lib/logging";`

2. **Wrap/update catch blocks** to use handleApiError:
```typescript
} catch (err) {
  return handleApiError(err, "api/marketplace/{sub-path}");
}
```

3. **Replace console.error/warn** with structured logging:
- `auctions/route.ts` (1 console.error): replace with systemLog
- `listings/route.ts` (1 console.error): replace with systemLog
- `orders/route.ts` (1 console.error): replace with systemLog
- `orders/[id]/route.ts` (2 console.error): replace with systemLog
- `orders/[id]/messages/route.ts` (1 console.error): replace with systemLog

4. **Source naming**: `"api/marketplace/{sub-path}"` (e.g., `"api/marketplace/auctions"`, `"api/marketplace/orders"`)

5. **For routes without try/catch**: Add wrapping try/catch around handler body.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 18 marketplace route files use handleApiError. All console.error/warn replaced with structured logging.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate seller, webhook, and remaining routes + final verification of zero console.error/warn across all API routes</name>
  <files>
    src/app/api/seller/earnings/route.ts,
    src/app/api/seller/withdraw/route.ts,
    src/app/api/webhooks/chain-deposits/route.ts
  </files>
  <action>
1. **seller/earnings/route.ts**: Add handleApiError import, wrap in try/catch with source `"api/seller/earnings"`.

2. **seller/withdraw/route.ts**: Add handleApiError import, wrap in try/catch with source `"api/seller/withdraw"`.

3. **webhooks/chain-deposits/route.ts** (7 console.error calls — the most in any route): This is a critical payment webhook.
   - Create `const log = createTaggedLogger("webhook/chain-deposits");`
   - Replace all 7 console.error calls with `log.error(...)` calls, passing structured metadata
   - The `.catch(() => ({}))` on line 52 (request.json) is acceptable — it's parsing fallback, not a silent error swallow. Leave it or convert to `.catch(() => { log.warn("Invalid JSON body"); return {}; })`
   - Wrap outer handler in handleApiError

4. **Final sweep**: After all changes, verify NO console.error or console.warn remains in any file under `src/app/api/`:
```bash
grep -r "console\.error\|console\.warn" src/app/api/ --include="*.ts"
```
If any remain, fix them.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 3 remaining route files migrated. Zero console.error/warn across all 68 API route files. Every route uses handleApiError for error responses.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes clean
- `grep -r "console\.error\|console\.warn" src/app/api/ --include="*.ts"` returns no matches
- `grep -r "handleApiError" src/app/api/ --include="*.ts" | wc -l` shows 60+ usages across all route categories
</verification>

<success_criteria>
- All marketplace, seller, and webhook routes use handleApiError
- All console.error/warn in API routes replaced with structured logging
- Zero console.error/warn across all 68 API route files
- Consistent { error: string } response shape
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-error-handling-logging/07-04-SUMMARY.md`
</output>
