---
phase: 07-error-handling-logging
plan: 05
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-04"]
files_modified:
  - src/components/charts/trading-chart.tsx
  - src/components/charts/top-movers.tsx
  - src/components/charts/rank-timeline.tsx
  - src/components/charts/quality-price-frontier.tsx
  - src/components/charts/benchmark-heatmap.tsx
  - src/components/models/model-overview.tsx
  - src/components/models/deploy-tab.tsx
  - src/components/marketplace/view-tracker.tsx
  - src/components/marketplace/purchase-button.tsx
  - src/components/marketplace/english-bid-panel.tsx
  - src/components/marketplace/dutch-bid-panel.tsx
  - src/components/marketplace/seller-orders-table.tsx
  - src/components/marketplace/listing-reviews.tsx
  - src/components/marketplace/contact-form.tsx
  - src/components/marketplace/edit-listing-form.tsx
  - src/components/marketplace/create-listing-form.tsx
  - src/components/layout/market-ticker.tsx
  - src/components/pwa-register.tsx
  - src/components/auth/auth-provider.tsx
  - src/app/(marketplace)/dashboard/seller/seller-dashboard-content.tsx
  - src/app/(marketplace)/dashboard/seller/listings/[slug]/edit/edit-listing-content.tsx
  - src/app/(auth)/wallet/wallet-content.tsx
  - src/app/(marketplace)/marketplace/auctions/[id]/auction-detail-content.tsx
  - src/app/(static)/contact/contact-content.tsx
  - src/hooks/use-earnings-data.ts
autonomous: true
requirements:
  - ERR-01
  - ERR-03

must_haves:
  truths:
    - "No silent .catch(() => {}) handlers exist in client components"
    - "All client components with fetch calls surface error state to users"
    - "Components show a visible error message when API calls fail"
    - "Non-critical catches (pwa-register, view-tracker) log to console.warn at minimum"
  artifacts:
    - path: "src/components/charts/trading-chart.tsx"
      provides: "Chart component with error state display"
      contains: "setError"
    - path: "src/components/marketplace/purchase-button.tsx"
      provides: "Purchase button with visible error messaging"
      contains: "error"
    - path: "src/components/layout/market-ticker.tsx"
      provides: "Ticker with silent catch replaced by console.warn"
      contains: "console.warn"
  key_links:
    - from: "src/components/charts/trading-chart.tsx"
      to: "user interface"
      via: "error state rendering"
      pattern: "error.*&&|error.*\\?"
    - from: "src/components/marketplace/purchase-button.tsx"
      to: "user interface"
      via: "error message display"
      pattern: "setError|error.*message"
---

<objective>
Fix all client-side silent catch blocks and ensure every component that makes API calls surfaces error state to users with a visible message.

Purpose: Satisfy ERR-01 (no silent catches anywhere) and ERR-03 (client components surface error state visibly).
Output: All 25 client component/page files with proper error handling and visible error messages.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-error-handling-logging/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix silent catches and add error state to chart, model, and layout components</name>
  <files>
    src/components/charts/trading-chart.tsx,
    src/components/charts/top-movers.tsx,
    src/components/charts/rank-timeline.tsx,
    src/components/charts/quality-price-frontier.tsx,
    src/components/charts/benchmark-heatmap.tsx,
    src/components/models/model-overview.tsx,
    src/components/models/deploy-tab.tsx,
    src/components/layout/market-ticker.tsx,
    src/components/pwa-register.tsx,
    src/components/auth/auth-provider.tsx
  </files>
  <action>
For each component, apply the appropriate error handling pattern based on criticality:

**Chart components (trading-chart, top-movers, rank-timeline, quality-price-frontier, benchmark-heatmap):**
These display data from API calls. Each needs:
1. Add an `error` state: `const [error, setError] = useState<string | null>(null);`
2. In catch blocks, set the error: `setError("Failed to load chart data")`
3. Render error state: Add a conditional render showing a simple error message when `error` is set:
```tsx
if (error) return <p className="text-sm text-red-500 p-4">{error}</p>;
```
4. For `trading-chart.tsx` line 70: `.catch(() => setLoading(false))` — change to `.catch((err) => { setError("Failed to load chart data"); setLoading(false); })`
5. For `top-movers.tsx` line 33: `.catch(() => {})` (fully silent) — change to `.catch(() => setError("Failed to load top movers"))`

**Model components (model-overview, deploy-tab):**
Same pattern as charts — add error state, surface in UI.
- `model-overview.tsx` line 41: `.catch(() => setLoading(false))` — add error state
- `deploy-tab.tsx` line 87: `.catch(() => setLoading(false))` — add error state

**Non-critical components:**
- `market-ticker.tsx` line 24: `.catch(() => {})` — this is cosmetic ticker data. Replace with `.catch(() => { console.warn("[market-ticker] Failed to fetch ticker data"); })`. No visible error needed since ticker failure should just show empty/stale data gracefully.
- `pwa-register.tsx` line 14: `.catch(() => {` — service worker registration failure is non-critical. Replace with `.catch((err) => { console.warn("[pwa-register] SW registration failed:", err); })`.
- `auth-provider.tsx`: Check its catch blocks. If they have error handling already, ensure it surfaces to user. If bare catch, add appropriate error state.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 10 chart/model/layout components have proper error handling. Silent catches eliminated — critical components show error messages, non-critical log warnings. No .catch(() => {}) remains in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Fix error handling in marketplace components and content pages</name>
  <files>
    src/components/marketplace/view-tracker.tsx,
    src/components/marketplace/purchase-button.tsx,
    src/components/marketplace/english-bid-panel.tsx,
    src/components/marketplace/dutch-bid-panel.tsx,
    src/components/marketplace/seller-orders-table.tsx,
    src/components/marketplace/listing-reviews.tsx,
    src/components/marketplace/contact-form.tsx,
    src/components/marketplace/edit-listing-form.tsx,
    src/components/marketplace/create-listing-form.tsx,
    src/app/(marketplace)/dashboard/seller/seller-dashboard-content.tsx,
    src/app/(marketplace)/dashboard/seller/listings/[slug]/edit/edit-listing-content.tsx,
    src/app/(auth)/wallet/wallet-content.tsx,
    src/app/(marketplace)/marketplace/auctions/[id]/auction-detail-content.tsx,
    src/app/(static)/contact/contact-content.tsx,
    src/hooks/use-earnings-data.ts
  </files>
  <action>
For each component, check existing catch blocks and ensure errors surface to users:

**Marketplace components with catch blocks:**
- `view-tracker.tsx`: Non-critical view tracking. Replace silent catch with `console.warn("[view-tracker] Failed to record view")`. No visible error needed.
- `purchase-button.tsx`: Already has some error handling for the `.json().catch(() => ({}))` on errData. Check if the main fetch catch surfaces errors. If not, add `setError("Purchase failed. Please try again.")` in catch.
- `english-bid-panel.tsx`: Has `.json().catch(() => ({}))` for error data parsing — this is fine. Ensure the main catch surfaces error message to user.
- `dutch-bid-panel.tsx`: Same pattern as english-bid-panel. Ensure catch surfaces error.
- `seller-orders-table.tsx`: Check catch blocks, add error state if missing.
- `listing-reviews.tsx`: Check catch blocks, add error state.
- `contact-form.tsx`: Check catch blocks, show error to user.
- `edit-listing-form.tsx`: Check catch blocks, show error message.
- `create-listing-form.tsx`: Check catch blocks, show error message.

**Content page components:**
- `seller-dashboard-content.tsx` line 58: `.catch(() => setLoading(false))` — add error state and display
- `edit-listing-content.tsx` line 34: `.catch(() => setFetchLoading(false))` — add error state
- `wallet-content.tsx`: Check catches, ensure errors surface
- `auction-detail-content.tsx`: Check catches, ensure errors surface
- `contact-content.tsx`: Check catches, ensure form errors surface

**Hook:**
- `use-earnings-data.ts` line 132: `.json().catch(() => ({}))` — this is response parsing fallback, acceptable. But ensure the hook exposes an `error` state that consuming components can display.

**Error display pattern for forms:**
```tsx
{error && <p className="text-sm text-red-500 mt-2">{error}</p>}
```

**Error display pattern for data pages:**
```tsx
if (error) return <div className="text-center text-red-500 py-8">{error}</div>;
```

After all changes, run a final sweep:
```bash
grep -rn "\.catch(() => {})" src/components/ src/app/ src/hooks/
```
Should return zero matches.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All 15 marketplace component/page files have proper error handling. No silent .catch(() => {}) anywhere in src/components/ or src/app/. All components that fetch data surface error state visibly to the user. Only non-critical catches (view-tracker, pwa-register, market-ticker) use console.warn instead of UI error.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes clean
- `grep -rn "\.catch(() => {})\|\.catch((_) => {})" src/` returns no matches
- `grep -rn "\.catch(() => {" src/components/ src/app/ src/hooks/` returns only the console.warn variants (view-tracker, pwa-register, market-ticker)
- All chart components have error state rendering
- All form components show error messages on failure
</verification>

<success_criteria>
- No silent .catch(() => {}) handlers exist anywhere in the codebase
- All data-fetching client components show visible error messages on failure
- Non-critical operations (view tracking, PWA, ticker) log warnings instead of swallowing silently
- TypeScript compiles cleanly
- ERR-01 and ERR-03 fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-error-handling-logging/07-05-SUMMARY.md`
</output>
