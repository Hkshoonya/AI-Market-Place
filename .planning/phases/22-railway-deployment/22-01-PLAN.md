---
phase: 22-railway-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/health/route.ts
  - src/lib/env.ts
  - src/middleware.ts
  - .env.example
  - src/app/(static)/api-docs/api-docs-content.tsx
  - src/app/(static)/contact/contact-content.tsx
  - src/lib/data-sources/adapters/openrouter-models.ts
autonomous: true
requirements:
  - DEPL-05
  - DEPL-04

must_haves:
  truths:
    - "GET /api/health returns 200 with status, version, timestamp for unauthenticated requests"
    - "GET /api/health with Bearer CRON_SECRET returns detailed response including DB connectivity, uptime, and pipeline summary"
    - "GET /api/health returns 503 when database is unreachable"
    - "All aimarketcap.com references in source code are updated to aimarketcap.tech"
    - "www.aimarketcap.tech redirects to https://aimarketcap.tech with 301"
  artifacts:
    - path: "src/app/api/health/route.ts"
      provides: "Health check endpoint with public/authenticated response tiers"
      exports: ["GET"]
    - path: "src/middleware.ts"
      provides: "www redirect logic alongside existing auth middleware"
      contains: "www.aimarketcap.tech"
  key_links:
    - from: "src/app/api/health/route.ts"
      to: "supabase"
      via: "createAdminClient SELECT 1 query"
      pattern: "createAdminClient"
    - from: "src/app/api/health/route.ts"
      to: "/api/pipeline/health"
      via: "reuses pipeline health summary counts"
      pattern: "computeStatus|pipeline"
    - from: "src/middleware.ts"
      to: "www redirect"
      via: "hostname check before auth logic"
      pattern: "www\\."
---

<objective>
Create the /api/health endpoint and update all domain references from aimarketcap.com to aimarketcap.tech.

Purpose: DEPL-05 requires a health endpoint for external monitoring. DEPL-04 requires the correct domain across the codebase. The www redirect ensures a single canonical URL.

Output: Working /api/health route, updated domain references, www redirect in middleware.
</objective>

<execution_context>
@C:/Users/Harshit/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Harshit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-railway-deployment/22-CONTEXT.md

@src/app/api/pipeline/health/route.ts
@src/lib/env.ts
@src/middleware.ts
@src/lib/pipeline/startup.ts
@package.json

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/api-error.ts:
```typescript
export function handleApiError(err: unknown, context: string): NextResponse;
```

From src/lib/logging.ts:
```typescript
export function createTaggedLogger(tag: string): Logger;
```

From src/lib/supabase/admin.ts:
```typescript
export function createAdminClient(): SupabaseClient;
```

From src/lib/pipeline-health-compute.ts:
```typescript
export function computeStatus(row: { consecutive_failures: number; last_success_at: string | null; expected_interval_hours: number }): "healthy" | "degraded" | "down";
```

From src/app/api/pipeline/health/route.ts (auth pattern to reuse):
```typescript
const authHeader = request.headers.get("authorization");
const cronSecret = process.env.CRON_SECRET;
const isAuthenticated = Boolean(cronSecret && authHeader === `Bearer ${cronSecret}`);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create /api/health endpoint</name>
  <files>src/app/api/health/route.ts, src/app/api/health/__tests__/route.test.ts</files>
  <behavior>
    - Public GET returns 200 with { status: "healthy"|"degraded", version: string, timestamp: string }
    - Authenticated GET (Bearer CRON_SECRET) returns 200 with extended body: { status, version, timestamp, uptime: number, database: { connected: boolean, latencyMs: number }, cron: { active: boolean, jobCount: number }, pipeline: { healthy: number, degraded: number, down: number } }
    - When DB query fails, returns 503 with { status: "unhealthy", version, timestamp, error: string }
    - Version comes from package.json (read at module scope, not per-request)
    - Uptime uses process.uptime() — returns seconds as number
  </behavior>
  <action>
    Create `src/app/api/health/route.ts` following the exact auth pattern from `/api/pipeline/health/route.ts`:
    - `export const dynamic = "force-dynamic";`
    - Use `createAdminClient()` for DB check — run `SELECT 1` and measure latency with `performance.now()`
    - Use `handleApiError` for error handling, `createTaggedLogger("health")` for logging
    - Define Zod schemas: HealthPublicSchema and HealthDetailSchema for response validation
    - For pipeline summary in detailed response: query data_sources + pipeline_health tables and count statuses using computeStatus from `@/lib/pipeline-health-compute` (same logic as pipeline/health route but only returning counts, not full adapter list)
    - For cron status: export a getter function placeholder that custom-server.js will populate via a setter — `let cronActive = false; let cronJobCount = 0; export function setCronStatus(active: boolean, count: number) {...}; export function getCronStatus() {...}` — OR simply report { active: false, jobCount: 0 } and Plan 02 will wire the real values. Use a module-level variable approach.
    - Version: `import pkg from "../../../../package.json" with { type: "json" };` or read via fs at module scope. Use the simpler import approach if Next.js supports it; otherwise use `JSON.parse(readFileSync(...))` at module scope.
    - Return 503 ONLY when DB is unreachable; degraded pipeline or cron returns 200 with status: "degraded"
  </action>
  <verify>
    <automated>npx vitest run src/app/api/health/__tests__/route.test.ts</automated>
  </verify>
  <done>
    - GET /api/health returns 200 with public schema (status, version, timestamp)
    - GET /api/health with valid Bearer returns 200 with full detail schema
    - DB failure returns 503
    - All response fields match Zod schemas
  </done>
</task>

<task type="auto">
  <name>Task 2: Update domain references and add www redirect</name>
  <files>src/lib/env.ts, src/middleware.ts, .env.example, src/app/(static)/api-docs/api-docs-content.tsx, src/app/(static)/contact/contact-content.tsx, src/lib/data-sources/adapters/openrouter-models.ts, .planning/PROJECT.md</files>
  <action>
    1. **Global domain update** — Replace all `aimarketcap.com` with `aimarketcap.tech` in source files:
       - `src/lib/env.ts` line 52: change fallback from `"https://aimarketcap.com"` to `"https://aimarketcap.tech"`
       - `.env.example` line 66: update comment from `aimarketcap.com` to `aimarketcap.tech`
       - `src/app/(static)/api-docs/api-docs-content.tsx`: update all `aimarketcap.com` URLs to `aimarketcap.tech` (lines 347, 738, 742)
       - `src/app/(static)/contact/contact-content.tsx` line 216: update email display (keep `support@aimarketcap.com` if that's the actual email, or update to `.tech` — use `.tech` to match domain)
       - `src/lib/data-sources/adapters/openrouter-models.ts` lines 258, 456: update HTTP-Referer header to `https://aimarketcap.tech`
       - `src/app/(static)/terms/page.tsx`: update all `support@aimarketcap.com` email references to `support@aimarketcap.tech`
       - `src/app/(static)/privacy/page.tsx`: update all `support@aimarketcap.com` email references to `support@aimarketcap.tech`
       - `.planning/PROJECT.md` line 77: update domain reference

    2. **www redirect in middleware.ts** — Add www-to-apex redirect BEFORE the Supabase session logic (at the top of the `middleware` function, after receiving the request):
       ```typescript
       // www -> apex redirect (must be first — before session handling)
       const host = request.headers.get("host") || "";
       if (host.startsWith("www.")) {
         const url = request.nextUrl.clone();
         url.host = host.replace(/^www\./, "");
         url.protocol = "https";
         return NextResponse.redirect(url, 301);
       }
       ```
       This goes inside the `middleware()` function before the `let supabaseResponse` line. No need to touch the matcher config — it already matches all non-static routes.

    Do NOT update planning docs like ROADMAP.md or REQUIREMENTS.md (they reference the old domain in a historical context, and the ROADMAP will be updated at the end of the phase).
  </action>
  <verify>
    <automated>npx vitest run && npm run lint</automated>
  </verify>
  <done>
    - Zero instances of aimarketcap.com in src/ directory (except possibly email addresses if we decide to keep .com email)
    - env.ts default site URL is https://aimarketcap.tech
    - middleware.ts contains www redirect logic before auth handling
    - Lint passes with zero warnings
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/app/api/health/` — health endpoint tests pass
2. `npm run lint` — zero warnings
3. `grep -r "aimarketcap\.com" src/` — returns zero results (domain fully updated)
</verification>

<success_criteria>
- /api/health endpoint exists with public and authenticated response tiers
- All source code references use aimarketcap.tech domain
- www redirect is in middleware before auth logic
- All tests pass, lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/22-railway-deployment/22-01-SUMMARY.md`
</output>
